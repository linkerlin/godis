# Godis × Redis 8.x 兼容性改进计划

> 评估基准：Redis 8.0 GA（2025-05）+ Redis 8.2（2025-08，新增 XDELEX/XACKDEL/BITOP 扩展操作符）+ Redis 8.4（VRANGE）
> 评估方法：逐命令源码核对（注册名 / arity / 选项 / 错误串 / 回复类型 / 边界行为），对照 Redis 官方文档与 redis/redis 仓库实现
> 评估日期：2026-07-21
> 评估范围：`database/`、`redis/protocol/`、`redis/parser/`、`aof/`、`cluster/`、`config/`、`scripting/`、`pubsub/`、`datastruct/`

---

## 目录

- [一、优先级定义](#一优先级定义)
- [二、总体评估概览](#二总体评估概览)
- [三、P0 严重问题速览（必须优先修复）](#三p0-严重问题速览必须优先修复)
- [四、分模块详细不兼容清单](#四分模块详细不兼容清单)
  - [4.1 RESP 协议层](#41-resp-协议层)
  - [4.2 错误消息规范](#42-错误消息规范)
  - [4.3 String 命令](#43-string-命令)
  - [4.4 Keys / Expiration 命令](#44-keys--expiration-命令)
  - [4.5 List 命令](#45-list-命令)
  - [4.6 Hash 命令](#46-hash-命令)
  - [4.7 Set 命令](#47-set-命令)
  - [4.8 Sorted Set 命令](#48-sorted-set-命令)
  - [4.9 Bitmap 命令](#49-bitmap-命令)
  - [4.10 Geo 命令](#410-geo-命令)
  - [4.11 HyperLogLog 命令](#411-hyperloglog-命令)
  - [4.12 Stream 命令](#412-stream-命令)
  - [4.13 JSON 命令](#413-json-命令)
  - [4.14 Vector Set 命令](#414-vector-set-命令命名与语法完全偏离)
  - [4.15 TimeSeries 命令](#415-timeseries-命令)
  - [4.16 概率数据结构](#416-概率数据结构)
  - [4.17 RediSearch 命令](#417-redisearch-命令)
  - [4.18 事务](#418-事务)
  - [4.19 发布订阅](#419-发布订阅)
  - [4.20 脚本与函数](#420-脚本与函数)
  - [4.21 连接与服务器命令](#421-连接与服务器命令)
  - [4.22 ACL](#422-acl)
  - [4.23 持久化（AOF / RDB）](#423-持久化aof--rdb)
  - [4.24 复制](#424-复制)
  - [4.25 集群](#425-集群)
  - [4.26 配置](#426-配置)
  - [4.27 SORT 与其他通用命令](#427-sort-与其他通用命令)
- [五、实施路线与里程碑](#五实施路线与里程碑)
- [六、验证策略](#六验证策略)
- [附录 A：缺失命令总表](#附录-a缺失命令总表)
- [附录 B：错误串差异对照表](#附录-b错误串差异对照表)

---

## 一、优先级定义

| 级别 | 含义 | 判定标准 |
|------|------|----------|
| **P0** | 阻断性 / 数据正确性 | 会导致 panic 崩溃、数据损坏、协议格式错误、或与 Redis 核心语义根本背离（客户端无法解析回复） |
| **P1** | 重要功能缺失 / 行为偏差 | Redis 8.x 核心命令或选项缺失；错误串不匹配导致客户端库解析失败；回复类型不符；阻塞命令不阻塞 |
| **P2** | 边缘行为差异 | 罕见路径返回值细节；命名规范；性能编码缺失；非关键的 quirk |
| **P3** | 增强 / 互通性 | 与真实 Redis 互操作（RDB 互通、集群协议互通）等可延后的改进 |

---

## 二、总体评估概览

| 维度 | 评估结论 |
|------|----------|
| **RESP 协议** | RESP3 含 blob error `!`；**回复降级机制不全**（Map/Set/Push 在 RESP2 连接上不降级） |
| **命令覆盖** | 基础五大类型较好；M2 批次已补大量 Stream/JSON/FT/概率/连接命令；Vector 命名仍有双名历史包袱 |
| **语义正确性** | List/ZSet/Stream **阻塞命令已真阻塞**（等待队列 + 写路径唤醒）；部分历史 panic/NaN 项已在 M1 处理 |
| **错误串规范** | 大量已对齐；仍有边缘不一致 |
| **持久化** | RDB 新类型有改进但仍有缺口；DUMP/RESTORE 朝标准 footer 靠拢 |
| **复制** | PSYNC2 基本兼容；**WAIT 已有简版**；与 Redis 7/8 互主从仍缺能力协商 |
| **集群** | **根本性不兼容**：1024 槽 + CRC32（Redis 是 16384 + CRC16），无 MOVED/ASK（**延期**） |
| **事务** | **EXEC 已按 Redis 语义**（出错继续、无自动回滚）；WATCH 返回 nil |
| **结论** | 单机兼容性较 2026-07 初评明显提升；集群/HLL 互通/完整搜索仍为主要缺口 |

**问题计数（初评粗略，部分已消化）：原 P0 ≈ 45 / P1 ≈ 90；以下方「进度更新」与分模块表格中的 ✅ 为准。**

### 进度更新（2026-07-24）

> 分支工作：`cursor/redis8-compat-m2h-m2l` 等。说「继续」按批次推进；集群槽位/HLL 编码互通/真 HNSW **明确延期**。

| 批次 | 状态 | 摘要 |
|------|------|------|
| **M1** | ✅ | panic/NaN/EXEC/WATCH、**BLPOP/BZPOP 真阻塞**（`list_block.go` 等待队列 + channel）、位序 MSB 等 |
| **M2 核心～M2l** | ✅ | EXPIRE/SET 选项、SCAN/HLL/GEO、TS/JSON/Stream/BF、CLIENT INFO、MEMORY、FLUSH ASYNC、REPLICAOF 等 |
| **M2m–M2q** | ✅ | CLIENT LIST/KILL/NO-EVICT、OBJECT、JSON.GET 格式、FT.ALIAS/TAGVALS/ALTER/EXPLAIN/CONFIG、TDIGEST.*、PSUBSCRIBE、SHUTDOWN、RESP3 `!`、BF/TOPK 选项等 |
| **M2r** | ✅ | 订阅态白名单（PS-3）、MONITOR 流式、CLIENT UNBLOCK、FT.PROFILE 最小集 |
| **M2s** | ✅ | XREAD/XREADGROUP 真阻塞（XADD 唤醒）+ `$` 一次性解析；FAILOVER 最小子集 |
| **M2t** | ✅ | 配置布尔/CONFIG SET 扩展、XAUTOCLAIM MINID、TRACKING OPTIN/NOLOOP、PFDEBUG PERIOD、BGSAVE→LASTSAVE |
| **M2u** | ✅ | maxmemory 写路径 OOM/淘汰闭环、CONFIG 同步、CF-3 常用指令桩、OBJECT IDLETIME/FREQ |
| **M2v** | ✅ | Sharded Pub/Sub 真连接+SCHANNELS、EVAL prepare（MULTI+加锁）、VRANDMEMBER、save/tcp-backlog 配置桩 |
| **M2w** | ✅ | VSETATTR/VGETATTR/VLINKS/VRANGE、timeout 空闲踢线、REPLCONF GETACK、CLIENT NO-TOUCH/REPLY |
| **M2x** | ✅ | FCALL execWithLock、COPY/MOVE 深拷贝、VSIM WITHATTRIBS、XSETID MAXDELETEDID、Unknown subcommand 句式 |
| **M2y** | ✅ | HGETEX/HSETEX/HGETDEL FIELDS、ACL DRYRUN 检 key、PFCOUNT 多键锁、CLIENT age/idle |
| **M2z** | ✅ | CLIENT INFO 对齐 LIST age/idle、XDELEX/XACKDEL 逐 ID 数组、undoHGetEx FIELDS、JSON.SET/DEL undo、FCALL→gopher-lua（FN-1） |
| **M2aa** | ✅ | 协议错关连接（P-3）、FUNCTION Engine 从 shebang（FN-4）、TS FILTER 存在性+DataEntity 遍历修复（TS-6）、配置引号解析（CF-1） |
| **M2ab** | ✅ | GEOSEARCH 含自身（G-6）、GEOHASH/GEORADIUS 缺键回复（G-7）、UnknownErr 文案（E-2）、XREADGROUP DeliveryCount+XPENDING `-`/`+`（ST-10）、REPLCONF capa/port（RP-2） |
| **M2ac** | ✅ | SINTERCARD LIMIT 提前终止（SE-5）、GEORADIUS 选项走 GEOSEARCH（G-5）、GEOSEARCH `sort.Slice`（G-8）、TS 聚合 **twa**（TS-5） |
| **M2ad** | ✅ | TS DUPLICATE_POLICY/ON_DUPLICATE（TS-4）、XGROUP SETID ENTRIESREAD（ST-11）、VSIM FILTER 最小表达式（V-3） |
| **M2ae** | ✅ | save 点位自动 BGSAVE（PE-4）、52-bit geohash 无损 float64（G-2）、环形 repl backlog（RP-3） |
| **M2af** | ✅ | hash field TTL opaque RDB/DUMP（PE-5）、CONFIG RESETSTAT（CF-6）、expire 错误串（E-4）、XPENDING TIME（ST-13） |
| **M2ag** | ✅ | MEMORY USAGE ExpireDict（SV-12）、CONFIG REWRITE（CF-6）、FUNCTION DUMP 二进制（FN-3）、TS.INCRBY RETENTION/LABELS（TS-7） |
| **M2ah** | ✅ | CLIENT TRACKING RESP2+REDIRECT/GETREDIR（SV-16）、RESTORE IDLETIME/FREQ、PING Bulk、OBJECT bloomfilter |
| **M2ai** | ✅ | MEMORY USAGE SAMPLES 采样估算、LCS 缺键当空串、Protocol error 双引号、SRANDMEMBER/SimpleDict 随机采样 |
| **M2aj** | ✅ | DUMP opaque 扩展 stream/JSON/vector/TS、FT.CREATE ON HASH|JSON、CLIENT LIST TYPE/flags/psub |
| **M2ak** | ✅ | CLIENT PAUSE 执行路径生效、INFO ops/sec + pubsub 计数、SCRIPT/FUNCTION FLUSH SYNC|ASYNC 校验 |
| **M2al** | ✅ | CLIENT KILL TYPE/USER、HELLO 真实 id、ROLE、DUMP opaque Bloom/HLL |
| **M2am** | ✅ | CLIENT KILL LADDR/MAXAGE、CLIENT LIST laddr、HELLO role 随主从、DUMP opaque Cuckoo |
| **M2an** | ✅ | INFO sync_full/partial_* 计数、DUMP opaque CMS/TopK/TDigest |
| **M2ao** | ✅ | INFO blocked_clients 含 ZSet 阻塞、OBJECT HLL/vector、XINFO FULL COUNT、FT.SEARCH 未知选项报错+VERBATIM/FILTER、pubsub RESP3 null |
| **M2ap** | ✅ | MEMORY HELP、CLIENT 未知子命令句式、CF EXPANSION 接入扩容、WAIT 催 GETACK、CLIENT 死桩转发 registry |
| **M2aq** | ✅ | FT.ADD NOSAVE 跳过 AOF、TDIGEST.ADD VALUES/WEIGHTS、**M2 可落地项收口**（延期项见下） |
| **M2ar** | ✅ | ACL `%R~`/`%W~`/`%RW~`、Lua `cjson` encode/decode、JSON.SET/DEL 自动喂 FT ON JSON |
| **M2as** | ✅ | ACL `(...)` 选择器块 + `clearselectors`、`&` 频道 DRYRUN、Lua `cmsgpack` pack/unpack |
| **M2at** | ✅ | 运行时 ACL 键/频道校验、`clients_in_timeout_table`、SCAN `ERR Invalid argument`、删 `execJSONPath` 死桩 |
| **M2au** | ✅ | FT SCHEMA `$.path AS name`、Lua `bit` 最小集（band/bor/…） |
| **M2av** | ✅ | bit.tohex/rol/ror/bswap、SLAVEOF MULTI 错误串、JSON.SET 未知选项、FT DIALECT/WITHSORTKEYS |
| **M2aw** | ✅ | JSON.GET `$` 数组封装、SELECT MULTI ERR、CLIENT 无 conn 报错、INFO/CONFIG `hz` |
| **M2ax** | ✅ | COMMAND LIST、GETRANGE 缺键空串、Lua error/status_reply、OBJECT int、proto-max-bulk-len |
| **M2ay** | ✅ | JSON `$` NUMINCRBY/ARRLEN/OBJLEN 封装、VADD NX/XX/SETATTR、Lua `acl_check_cmd`、FT.SEARCH INKEYS |
| **M2az** | ✅ | protected-mode 拒绝非本机、FT.SEARCH HIGHLIGHT、Lua `set_repl`/`LOG_*`、tcp-keepalive 套接字 |
| **M2ba** | ✅ | FT 短语+SLOP/INORDER/TIMEOUT、JSON `$` STRAPPEND/ARR*/STRLEN、VINFO HNSW 桩、VADD 无害选项文档 |
| **M2bb** | ✅ | FT INFIELDS/SUMMARIZE、JSON `$` ARRPOP/OBJKEYS/ARRTRIM/ARRINDEX、Lua REDIS_VERSION/REPL_* |
| **M2bc** | ✅ | JSON `$` CLEAR/TOGGLE/DEBUG、FT.AGGREGATE LOAD*/VERBATIM/TIMEOUT/APPLY、INFO redis_mode + CLIENT LIST ssub、Lua set_error_handler |
| **M2bd** | ✅ | CONFIG loglevel 接线 logger、FT.EXPLAIN/PROFILE 浅层增强、Lua error_handler 回调、CLIENT LIST `user=` |
| **M2be** | ✅ | CONFIG logfile 真接线、FT RETURN AS + WITHCURSOR 明确拒绝、INFO maxmemory*、CLIENT LIST `ID`/`resp=`；顺带 Lua `redis.call` 识别全部错误回复以触发 error_handler |
| **M2bf** | ✅ | INFO `evicted_keys`+计数、CONFIG appendfsync/pidfile 真接线、CLIENT LIST `cmd=`、FT.AGGREGATE APPLY 明确拒绝 |
| **M2bg** | ✅ | INFO expired/keyspace 真计数、CLIENT LIST `multi=`、CONFIG dir/dbfilename、FT FILTER `@field`、Lua `set_repl(REPL_NONE)` 跳过 AOF |
| **M2bh** | ✅ | CLIENT LIST `watching=`/flags `b`/`t`、CONFIG aclfile + replica-read-only、INFO `total_error_replies`、FT TIMEOUT 软超时 |
| **M2bi** | ✅ | CLIENT LIST flags `e`、CONFIG appendfilename + appendonly 冷启 AOF、INFO pubsub/watching_clients、FT GEO 真过滤、Lua `set_repl` AOF/REPLICA 分路径 |

**文档勘误（已核实代码）：**

| 原描述 | 现状 |
|--------|------|
| P0-19/20、L-1「`time.Sleep` 假阻塞」 | **已过时** → `list_block.go` 真阻塞；`TestBLPopWokenByLPush` 覆盖 |
| PS-2 PSUBSCRIBE 缺失 | **已实现** |
| SV-1 SHUTDOWN / SV-5 RESET / P-2 blob `!` / RS-7 FT.CONFIG | **已实现** |
| 附录 A 大量「全缺」命令（BITOP、LPOS、SMOVE、WAIT、XCLAIM…） | **多数已实现**，见附录 A 修订 |

**仍未完 / 明确延期：** FAILOVER 完整协调；集群 CRC16/MOVED/ASK；HLL 与 Redis 互通；Vector 真 HNSW；完整 RediSearch 短语；精确 Redis 级内存会计与真 LFU clock；FUNCTION DUMP 官方二进制互通；MIGRATE。

**M2 结论（2026-07-25）：** 单机路径「小而真」兼容缺口已清至延期清单；M2ar–M2bi 消化延期子集（ACL、Lua、JSON `$`、VADD、FT、CONFIG 接线、CLIENT LIST、INFO 计数）。

---

## 三、P0 严重问题速览（必须优先修复）

> 以下问题会导致崩溃、数据损坏、或协议错误，应在第一周期内全部修复。

### 3.1 崩溃类（panic）

| # | 位置 | 问题 | Redis 行为 |
|---|------|------|-----------|
| P0-1 | `database/string.go` SetBit/GetBit | 负 offset 直接索引 `bm[负]` panic | 报 `ERR bit offset is not an integer or out of range` |
| P0-2 | `database/string.go` SetRange | 负 offset 不检查，`bytes[idx]` panic | 报 `ERR offset is out of range` |
| P0-3 | `database/string.go` BitCount | `BITCOUNT key start`（单下标）访问 `args[2]` panic | arity 校验拒绝 |
| P0-4 | `database/string.go` BitPos | `BITPOS key bit start`（缺 end）访问 `args[3]` panic | arity 校验拒绝 |
| P0-5 | `database/list.go` LPop/RPop | 负 count 时 `make([][]byte, count)` panic | 报 `ERR value is out of range, must be positive` |
| P0-6 | `datastruct/sortedset/border.go` ParseScoreBorder/ParseLexBorder | 空字符串边界 `s[0]` panic | 报错 |

### 3.2 数据损坏类

| # | 位置 | 问题 | 影响 |
|---|------|------|------|
| P0-7 | `datastruct/sortedset/skiplist.go` | **NaN 作为 score 入库**：ZADD/ZINCRBY 接受 "nan"；NaN 比较恒 false → 节点插头部、ZREM 删不掉形成幽灵节点、ZCARD 与 ZRANGE 不一致 | 跳表状态不可逆损坏 |
| P0-8 | `database/database.go` LTrim | trim 后列表为空**不删键**，留下空列表 | 内存泄漏；EXISTS 误报 |
| P0-9 | `database/set.go` SPop | 弹空后**不删键** | 同上 |
| P0-10 | `database/geo.go` execGeoSearchStore | 把成员以 **score=0** 落库（应是 geohash），STOREDIST 未实现，WITH* 选项丢失成员 | dest key 数据全损坏，后续 GEO 命令失真 |
| P0-11 | `database/keys.go` execRename | **src==dest 时删除键**；src 无 TTL 而 dest 有 TTL 时 **TTL 残留** | 数据丢失 / TTL 错乱 |
| P0-12 | `database/stream.go` execXTrim | 通过**插入假条目 `{9999999999999-0}`** 触发裁剪 | 污染流 |
| P0-13 | `database/json.go` + `jsonpath.go` | `JSON.ArrLen` 被 jsonpath.go 双重注册覆盖，prepare=nil → 事务/集群**不加锁** | 并发数据竞争 |
| P0-14 | `database/json.go` NumMultBy / StrLen | **占位实现**：NumMultBy 恒返回 OK 不改数据；StrLen 恒返回 0 | 静默错误结果 |

### 3.3 协议格式错误

| # | 位置 | 问题 | Redis 行为 |
|---|------|------|-----------|
| P0-15 | `database/sortedset.go` execZMPop | 回复把每个 `[member,score]` 对**先序列化成 RESP 字节再当 bulk 塞进外层数组** → 嵌套被二次编码 | 嵌套数组应为 `*2 key *n *2 m s ...` |
| P0-16 | `redis/protocol/resp3.go` | **Map/Set/Push/Attribute 回复在 RESP2 连接上不降级**，直接输出 `%..`/`~..`/`>..` 破坏 RESP2 | 应降级为 `*` 数组 |

### 3.4 事务语义根本错误

| # | 位置 | 问题 | Redis 行为 |
|---|------|------|-----------|
| P0-17 | `database/transaction.go` execMulti | 运行时错误**中断执行并回滚所有已执行命令**，整体返回 EXECABORT | **继续执行其余命令**，错误作为数组元素返回 |
| P0-18 | `database/transaction.go` execMulti | WATCH 键被修改返回 **空数组 `*0`** | 返回 **nil 多重批量 `*-1`**（RESP2）/ `_`（RESP3） |

### 3.5 阻塞命令（✅ M1 已修 List/ZSet；Stream 仍有缺口）

| # | 位置 | 问题 | 状态 |
|---|------|------|------|
| P0-19 | `database/list_block.go` BLPOP/BRPOP/BLMOVE/BLMPOP | 原：`time.Sleep` 假阻塞 | ✅ 等待队列 + `signalListWaiters`；timeout=0 无限等 |
| P0-20 | BZPOPMIN/BZPOPMAX/BZMPOP | 同上 | ✅ `signalZSetWaiters` |
| — | `stream_consumer.go` XREAD/XREADGROUP | ~~sleep 轮询~~ → **等待队列 + XADD 唤醒** | ✅ M2s |

### 3.6 模块级根本不兼容

| # | 模块 | 问题 |
|---|------|------|
| P0-21 | Vector Set | **命令命名完全错误**：注册 `VSAdd/VSGet/VSDel/VSSearch/...`，Redis 8.0 是 `VADD/VSIM/VREM/VDIM/VCARD/VEMB/VGETATTR/VINFO/VLINKS/VRANDMEMBER/VSETATTR`；语法、选项（FILTER/EF/EPSILON/COUNT/WITHSCORES/ELE/FP32/VALUES/TRUTH）全不兼容 |
| P0-22 | Bitmap | **位序 LSB-first vs Redis MSB-first**：同一命令产生互为位反转的字节串，根本性不兼容 |
| P0-23 | Bitmap | **BITOP 完全缺失**（含 Redis 8.2 的 DIFF/DIFF1/ANDOR/ONE）；**BITFIELD 完全缺失** |
| P0-24 | HyperLogLog | 自研 FNV-1a + 无大范围修正，**与 Redis PFADD/PFCOUNT 结果不互通**；独立类型非字符串编码 |
| P0-25 | Stream | **XINFO（STREAM/CONSUMERS/HELP）完全缺失**；`XGROUP CREATECONSUMER/DELCONSUMER`、`XINFO GROUPS` 注册名导致**标准空格形式无法解析** |
| P0-26 | 集群 | **槽位 1024 + CRC32**（Redis 是 16384 + CRC16）；无 MOVED/ASK；CLUSTER NODES/INFO/SLOTS 硬编码；无法用 `redis-cli --cluster` |
| P0-27 | 持久化 | RDB **不写 stream/JSON/bitmap/vector/timeseries/概率结构** → 重启丢全部新类型数据 |
| P0-28 | SORT | **占位实现**：DESC/ALPHA/BY pattern 不生效，无真正排序步骤 |

### 3.7 安全

| # | 位置 | 问题 |
|---|------|------|
| P0-29 | `database/acl.go` execACLGenpass | **GENPASS 是伪随机固定序列**（`chars[i%62]` 循环），输出恒为 `abcd...`，无任何随机性 |

---

## 四、分模块详细不兼容清单

### 4.1 RESP 协议层

涉及文件：`redis/parser/parser.go`、`redis/protocol/reply.go`、`redis/protocol/resp3.go`、`redis/server/std/server.go`

| # | 优先级 | 问题 | 现状 | Redis 8.x 行为 | 改进方案 |
|---|--------|------|------|---------------|----------|
| P-1 | P0 | Map/Set/Push/Attribute 回复不降级 | `ReplyToRESP3` 对这些类型直接返回自身 `ToBytes()`；RESP2 连接走 `result.ToBytes()` 分支也不降级 | RESP2 连接上 Map→array（扁平为 `[k,v,k,v...]`）、Set→array、Push→array | 在 `ReplyToRESP3` 增加参数：连接为 RESP2 时对 Map/Set/Push/Attribute 做降级转换 |
| P-2 | P1 | blob error `\!` RESP3 | ~~未解析~~ → **已解析**（`parseBlobError`） | ✅ | — |
| P-3 | P1 | 协议错误不关闭连接 | ~~仅回写错误并 continue~~ → **回写后关闭连接** | ✅ M2aa |
| P-4 | P2 | 协议错误前缀不规范 | ~~单引号~~ → **双引号** `-ERR Protocol error: \"<msg>\"` | ✅ M2ai |
| P-5 | P2 | NullBulkReply 在 RESP3 下靠 ReplyToRESP3 转 `_` | 大量命令直接用 NullBulkReply；若不经 ReplyToRESP3（如 SUBSCRIBE 直写连接）则在 RESP3 下输出 `$-1` 而非 `_\r\n` | RESP3 应统一 `_` | 审查所有直写连接的路径，RESP3 下转 Null |

---

### 4.2 错误消息规范

涉及文件：`redis/protocol/errors.go` 及全代码库

> 错误串不一致是**全局性**问题，许多客户端库依赖精确错误前缀做错误分类（如 redis-py 的 `ResponseError` 子类）。以下是高频出现的偏差：

| # | 优先级 | godis 输出 | Redis 输出 | 出现位置 |
|---|--------|-----------|-----------|----------|
| E-1 | P1 | `-Err syntax error` | `-ERR syntax error` | `SyntaxErrReply`，几乎所有命令的选项解析失败 |
| E-2 | P1 | `-Err unknown` | ~~`-ERR unknown`~~ → **`-ERR unknown error`**（panic 兜底） | ✅ M2ab |
| E-3 | P1 | `no such key`（无 ERR 前缀） | `-ERR no such key` | RENAME/RENAMENX src 不存在 |
| E-4 | P1 | `ERR invalid expire time in setex` | ~~setex 已对齐~~；**SET/GETEX** 亦对齐 `'set'`/`'getex' command` | ✅ M2af |
| E-5 | P1 | `please enable aof before using save` | （Redis 无此限制） | SAVE/BGSAVE 未开 AOF 时 |
| E-6 | P1 | `AppendOnly is false, you can't rewrite aof file` | （Redis 无此限制） | BGREWRITEAOF |
| E-7 | P2 | SETRANGE offset 非整数返回原始 `strconv.ParseInt: parsing "x": invalid syntax` | `ERR value is not an integer or out of range` | SETRANGE |
| E-8 | P2 | `Invalid section for 'info' command`（无 ERR） | `-ERR Invalid section for 'info' command` | INFO |
| E-9 | P2 | `Unknown subcommand '<sub>'`（无 ERR） | `-ERR Unknown subcommand or wrong number of arguments for '<sub>'` | COMMAND 未知子命令 |
| E-10 | P2 | Slowlog 未知子命令串含两个空格 `for  '<sub>'` | `for '<sub>'` | SLOWLOG |
| E-11 | P2 | SET 未知选项返回 `-Err syntax error` | `ERR syntax error` | SET EXAT/PXAT/KEEPTTL/GET |

**改进方案**：统一 `errors.go`，所有错误串加 `ERR ` 前缀（标准错误）或对应前缀（`WRONGTYPE`/`NOPERM`/`NOSCRIPT`/`EXECABORT`/`LOADING`/`MISCONF`/`BUSY`/`READONLY`/`NOAUTH`/`CLUSTERDOWN`/`CROSSSLOT`/`MOVED`/`ASK`/`WRONGPASS`）。

---

### 4.3 String 命令

涉及文件：`database/string.go`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| S-1 | P0 | INCR/DECR/INCRBY/DECRBY | **无溢出检查**，int64 回绕 | 计算前检查 `val+delta` 是否溢出，溢出报 `ERR increment or decrement would overflow` |
| S-2 | P0 | SETRANGE | 负 offset panic | 加 `offset<0` 校验报 `ERR offset is out of range` |
| S-3 | P0 | INCRBYFLOAT | 无 NaN/Inf 检查 | 计算后用 `math.IsNaN/IsInf` 检查，报 `ERR increment would produce NaN or Infinity` |
| S-4 | P1 | SET | 不支持 **EXAT/PXAT/KEEPTTL/GET** | 补全选项；GET 选项下返回旧值（nil 用 NullBulk） |
| S-5 | P1 | GETEX | 不支持 EXAT/PXAT；**未知选项静默忽略**（应报错） | 补全选项；解析循环加 else 报 syntax error |
| S-6 | P1 | SETRANGE | offset 解析失败返回原始 strconv 错误 | 改为 `ERR value is not an integer or out of range` |
| S-7 | P1 | GETRANGE/Substr | 键存在但范围无效返回 `NullBulkReply`（`$-1`） | 应返回空串 bulk（`$0\r\n`） |
| S-8 | P1 | MSet | **不清除旧 TTL**（PutEntity 不碰 ttlMap） | 写入前对每个 key 调 `db.Persist(key)` 或在 PutEntity 内清理 |
| S-9 | P1 | RANDOMKEY | 不检查过期，可能返回已过期未清理的键 | 取出后调 `IsExpired` 过滤 |
| S-10 | P2 | APPEND | 不存在键视为空串创建（与 Redis 一致，OK） | — |
| S-11 | P2 | SETEX | AOF 写 `setex`；PSETEX AOF 也写 `setex` | PSETEX 应写 `psetex` 或统一 `SET ... PX` |

---

### 4.4 Keys / Expiration 命令

涉及文件：`database/keys.go`、`database/dump.go`、`database/server.go`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| K-1 | P0 | RENAME | src==dest 删除键；src 无 TTL 而 dest 有 TTL 时 TTL 残留 | 加 src==dest 校验报 `ERR source and destination objects are the same`；迁移前清 dest TTL |
| K-2 | P0 | DUMP/RESTORE | 自定义 5 字节 header，**无 RDB 版本/CRC64**，仅支持 String | 用 `hdt3213/rdb` 生成标准 RDB payload + 2 字节版本 + 8 字节 CRC64（LFM）；支持全部类型 |
| K-3 | P1 | EXPIRE/PEXPIRE/EXPIREAT/PEXPIREAT | arity 固定 3，**不支持 NX/XX/GT/LT**（Redis 7.0） | 改为 `-3` arity，解析可选 flag |
| K-4 | P1 | WAIT | **完全缺失** | 实现：阻塞等待 N 个副本确认或超时 |
| K-5 | P1 | UNLINK | = DEL（同步），无异步释放 | 可接受，但应在注释说明；或接入 lazyfree |
| K-6 | P1 | KEYS/SCAN | glob 用正则翻译法（`lib/wildcard`），非严格 glob；正则元字符注入风险 | 重写为严格 glob 匹配（`*`/`?`/`[...]`），不透传正则 |
| K-7 | P1 | SCAN | **不过滤过期键**（DictScan 无 IsExpired 检查） | 扫描后过滤过期键 |
| K-8 | P2 | OBJECT ENCODING | 返回的编码名（无 listpack/ziplist/intset 概念）与 Redis 不一致 | 至少返回 `raw`/`hashtable`/`listpack` 等可比拟的占位编码名 |
| K-9 | P2 | EXISTS | 重复键重复计数（与 Redis 一致，OK） | — |
| K-10 | P2 | COPY/MOVE/SWAPDB | 在 server.go 内联分发（OK） | — |

---

### 4.5 List 命令

涉及文件：`database/list.go`、`database/list_block.go`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| L-1 | P0 | BLPOP/BRPOP/BLMOVE/BLMPOP | ~~假阻塞~~ → **已真阻塞**（`list_block.go`） | ✅；CLIENT UNBLOCK 已联动（M2r） |
| L-2 | P0 | LPOP/RPOP | 负 count panic | 加 `count<0` 校验报 `ERR value is out of range, must be positive` |
| L-3 | P0 | LTRIM | trim 后列表为空**不删键** | 空时 `db.Remove(key)` |
| L-4 | P1 | LPOS | **缺失**（Redis 6.0.6） | 实现：支持 RANK/COUNT/MAXLEN/ELEMENT |
| L-5 | P1 | BRPOPLPUSH | **缺失**（虽 deprecated 但广泛使用） | 实现（内部转 BLMOVE RIGHT LEFT） |
| L-6 | P1 | LPOP/RPOP | 带 count 且 key 不存在返回 nil 而非空数组 | key 不存在且带 count 返回 `*0` |
| L-7 | P1 | LMPOP | prepare 用错（`prepareReadKeys` 把最后参数当 timeout 排除，但 LMPOP 最后参数是 LEFT/RIGHT/count） | 重写 prepare 函数正确声明 numkeys 后的键 |
| L-8 | P2 | RPUSHX | 错误串写 `'rpush'` 而非 `'rpushx'` | 修正 |
| L-9 | P2 | LINSERT | 只查找第一个 pivot（与 Redis 一致，OK） | — |

---

### 4.6 Hash 命令

涉及文件：`database/hash.go`、`database/hash_expire.go`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| H-1 | P0 | HINCRBY | 返回 **BulkReply**（字符串）而非 **IntReply** | 改为 IntReply |
| H-2 | P0 | HINCRBY | 无溢出检查 | 加溢出检查 |
| H-3 | P0 | HINCRBYFLOAT | 无 NaN/Inf 检查；返回 Bulk（OK，Redis 也是 bulk） | 加 NaN/Inf 检查 |
| H-4 | P1 | HEXPIRETIME/HPEXPIRETIME | **缺失** | 实现 |
| H-5 | P1 | HEXPIRE 系列 | **不要求 `FIELDS numfields` 关键字**，与 Redis 7.4 语法不兼容 | 改为 `HEXPIRE key seconds [NX\|XX\|GT\|LT] FIELDS numfields field...` |
| H-6 | P1 | HTTL/HPTTL/HGETEX/HSETEX | ~~只支持单 field~~ → **HGETEX/HSETEX/HGETDEL 支持 FIELDS**（兼旧语法）；HSETEX 支持 FNX/FXX | ✅ M2y |
| H-7 | P1 | HEXPIRE 的 GT/LT | 无现有 TTL 字段一律判不满足（Redis 把无 TTL 视为无穷大，GT 满足） | 修正：无 TTL 时 GT 满足、LT 不满足 |
| H-8 | P1 | HSCAN | **假游标**（一次返回全部）；key 不存在返回 NullBulkReply 而非 `["0",[]]` | 实现真游标；统一返回格式 |
| H-9 | P1 | HGETDEL | undo 返回 nil（**回滚未实现**，值已丢失） | 实现回滚：保存旧值 |
| H-10 | P1 | HRANDFIELD | 无 count 时返回**数组**而非单值；key 不存在返回空数组而非 nil | 无 count 返回单 Bulk；key 不存在返回 nil |
| H-11 | P2 | HGETDEL | 单 field 返回 Bulk，多 field 返回数组（不统一） | 统一用 FIELDS 形式 |

---

### 4.7 Set 命令

涉及文件：`database/set.go`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| SE-1 | P0 | SPOP | **永远返回数组**（无 count 应返回单 Bulk）；弹空**不删键** | 无 count 返回单 Bulk；空时删键 |
| SE-2 | P0 | SRANDMEMBER | ~~无 count 数组 + 负 count 偏首元素~~ → **单 Bulk + SimpleDict/ConcurrentDict 真随机（可重复）** | ✅ M2ai |
| SE-3 | P1 | SMOVE | **缺失** | 实现 |
| SE-4 | P1 | SINTERSTORE | 空结果**不清除旧 dest**；不调 Remove 清 TTL（与 SUNIONSTORE/SDIFFSTORE 不一致） | 统一先 Remove(dest) 清 TTL |
| SE-5 | P1 | SINTERCARD | ~~先完整求交再截断~~ → **IntersectCard 遇 LIMIT 提前停** | ✅ M2ac |
| SE-6 | P1 | SSCAN | 假游标 | 实现真游标 |
| SE-7 | P2 | SPOP/SRANDMEMBER | 带 count 且 key 不存在返回 nil 而非空数组 | 返回 `*0` |

---

### 4.8 Sorted Set 命令

涉及文件：`database/sortedset.go`、`datastruct/sortedset/`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| Z-1 | P0 | ZADD | **完全不支持** NX/XX/GT/LT/CH/INCR | 补全全部选项；INCR 与 ZINCRBY 等价 |
| Z-2 | P0 | ZADD/ZINCRBY | **无 NaN 拒绝** → 幽灵节点、ZCARD/ZRANGE 不一致（数据损坏） | `ParseFloat` 后 `math.IsNaN` 拒绝，报 `ERR value is not a valid float`（Redis 对 "nan" 返回此错误） |
| Z-3 | P0 | ZMPOP | 回复**嵌套二次编码**（先序列化再当 bulk 塞） | 用 MultiRawReply 正确嵌套 |
| Z-4 | P0 | ZINTERSTORE/ZINTER | **AGGREGATE 被忽略**（只做加权求和，MIN/MAX 无效） | 实现 SUM/MIN/MAX 三种聚合 |
| Z-5 | P1 | ZRANGE | 不支持 **REV/BYSCORE/BYLEX/LIMIT**（Redis 6.2 统一语法） | 补全 |
| Z-6 | P1 | ZRANGESTORE | **缺失** | 实现 |
| Z-7 | P1 | ZRANDMEMBER | **缺失**（Redis 6.2） | 实现 WITHSCORES |
| Z-8 | P1 | ZRANK/ZREVRANK | 不支持 **WITHSCORE**（Redis 7.2） | 补全 |
| Z-9 | P1 | ZSCAN | 假游标；score 固定 10 位小数（应 `-1` 自适应） | 实现真游标；统一 score 格式 |
| Z-10 | P2 | ZREVRANGEBYSCORE/ZREVRANGEBYLEX | 参数不足错误串误写为 `'zrangebyscore'`/`'zrangebylex'` | 修正 |
| Z-11 | P2 | ZREVRANGE | WITHSCORES 大小写敏感（ZRange 不敏感） | 统一大小写不敏感 |
| Z-12 | P2 | ZRANGEBYLEX | 不存在 key 返回 `:0` 而非空数组 | 返回 `*0` |
| Z-13 | P2 | ZREMRANGEBYSCORE | 不存在 key 返回 `*0` 而非 `:0` | 返回 `:0` |

---

### 4.9 Bitmap 命令

涉及文件：`database/string.go`、`datastruct/bitmap/bitmap.go`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| B-1 | P0 | 全部位图命令 | **位序 LSB-first vs Redis MSB-first**：`SETBIT k 0 1` 在 godis 得 `0x01`，Redis 得 `0x80` | **改为 MSB-first**：`mask = 0x80 >> (offset%8)`；同步修改 ForEachBit/BitCount/BitPos |
| B-2 | P0 | SETBIT/GETBIT | 负 offset panic | 加校验 |
| B-3 | P0 | BITCOUNT/BITPOS | 单下标调用 panic | arity 校验 |
| B-4 | P0 | BITOP | **完全缺失**（AND/OR/XOR/NOT + Redis 8.2 的 DIFF/DIFF1/ANDOR/ONE） | 实现 |
| B-5 | P0 | BITFIELD | **完全缺失**（GET/SET/INCRBY/OVERFLOW WRAP\|SAT\|FAIL） | 实现 |
| B-6 | P1 | BITPOS | 不存在 key 恒返回 -1（bit=0 应返回 0）；范围无效返回 0（应返回 -1） | 修正返回值 |
| B-7 | P2 | BITCOUNT | 默认单位 BYTE（OK）；BIT 单位语义（OK） | — |

> **B-1 是最严重的位图问题**：即使修复前所有命令都"能用"，数据也与 Redis 不互通，迁移/复制时位图会全部错位。

---

### 4.10 Geo 命令

涉及文件：`database/geo.go`、`lib/geohash/`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| G-1 | P0 | GEOSEARCHSTORE | 成员以 **score=0** 落库（应为 geohash 或距离）；STOREDIST 未实现；WITH* 选项丢失成员 | 正确存储：无 STOREDIST 时 score=geohash，有 STOREDIST 时 score=距离 |
| G-2 | P0 | 全局 | ~~64 位 geohash→float64 精度损失~~ → **52-bit（对齐 Redis）**，score 可无损存 float64 | ✅ M2ae |
| G-3 | P1 | GEOADD | 不支持 NX/XX/CH；越界错误串经纬度顺序写反 | 补全选项；修正错误串 |
| G-4 | P1 | GEODIST/GEORADIUS | 只支持 m/km（无 mi/ft） | 补全单位 |
| G-5 | P1 | GEORADIUS | ~~无 WITH*/COUNT/STORE~~ → **转发 GEOSEARCH/STORE（haversine）** | ✅ M2ac |
| G-6 | P1 | GEOSEARCH | ~~FROMMEMBER 排除自身~~ → **包含自身**（ANY 已有） | ✅ M2ab |
| G-7 | P1 | GEOPOS/GEOHASH | ~~缺键 GEOHASH 单 nil~~ → **按成员数 null 数组**；GEORADIUS 空数组 | ✅ M2ab |
| G-8 | P2 | GEOSEARCH | ~~O(n²) 冒泡~~ → **`sort.Slice`** | ✅ M2ac |

---

### 4.11 HyperLogLog 命令

涉及文件：`database/hyperloglog.go`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| HLL-1 | P0 | 全局 | 自研 **FNV-1a + 无大范围修正**，与 Redis 不可互通；独立类型非 Redis 的字符串编码 | 改用 MurmurHash64A + Redis 的密集/稀疏编码 + 大范围修正；或明确声明"不互通"并在文档标注 |
| HLL-2 | P1 | PFADD | 返回值用估计值比较（应比较寄存器是否变化） | 改为寄存器 dirty 标志 |
| HLL-3 | P1 | PFMERGE | 强制至少 1 个源（Redis 允许 `PFMERGE dest` 单参数） | 允许单参数 |
| HLL-4 | P1 | PFDEBUG | ~~缺失~~ → **GETREG/DECODE/PERIOD**（PERIOD 为兼容桩） | ✅ |
| HLL-5 | P2 | PFCOUNT 多 key | ~~prepare 只锁第一个~~ → **readAllKeys** | ✅ M2y |

---

### 4.12 Stream 命令

涉及文件：`database/stream.go`、`database/stream_consumer.go`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| ST-1 | P0 | XINFO STREAM/CONSUMERS/HELP | **完全缺失**（`execXInfoStream` 是死代码） | 注册 `XINFO` 总命令，实现 STREAM [FULL COUNT]、CONSUMERS、HELP |
| ST-2 | P0 | XGROUP CREATECONSUMER/DELCONSUMER | 注册名 `XGroupCreateConsumer` 无分隔符，**标准空格形式无法解析** | 注册为 `XGroup.CreateConsumer`（点）或加入 manual 别名表 |
| ST-3 | P0 | XINFO GROUPS | 注册名 `XInfoGroups` 同上问题 | 同上 |
| ST-4 | P0 | XTRIM | 通过**插入假条目 `{9999999999999-0}`** 触发裁剪，污染流 | 重写为直接裁剪底层存储 |
| ST-5 | P1 | XCLAIM | **缺失** | 实现：支持 IDLE/TIME/RETRYCOUNT/FORCE/JUSTID |
| ST-6 | P1 | XAUTOCLAIM | ~~缺失~~ → **已实现**（COUNT/JUSTID/**MINID**） | ✅ M2t |
| ST-7 | P1 | XSETID（顶层） | ~~缺失~~ → **已实现**（ENTRIESADDED/**MAXDELETEDID 持久化**） | ✅ M2x |
| ST-8 | P1 | XDELEX/XACKDEL | ~~整数计数~~ → **逐 ID 数组**（-1/1/2，Redis 8.2） | ✅ M2z |
| ST-9 | P1 | XREAD/XREADGROUP | ~~sleep 轮询~~ → **真阻塞**（`stream_block.go` + `signalStreamWaiters`）；`$` 在命令开始时解析一次 | ✅ M2s |
| ST-10 | P1 | XREADGROUP | ~~历史 pending 空操作/DeliveryCount 覆写为 1~~ → **历史重读递增计数**；XPENDING 支持 `-`/`+` 嵌套行 | ✅ M2ab |
| ST-11 | P1 | XGROUP SETID | ~~仅 LastID~~ → **支持 ENTRIESREAD**；不清理 PEL（与 Redis 一致） | ✅ M2ad |
| ST-12 | P1 | XGROUP CREATE | ENTRIESREAD 解析后未生效 | 接入 group.EntriesRead |
| ST-13 | P2 | XPENDING 详细模式 | ~~无 IDLE/TIME~~ → **IDLE + TIME**（按 DeliveryTime unix ms 过滤） | ✅ M2af |

---

### 4.13 JSON 命令

涉及文件：`database/json.go`、`database/jsonpath.go`、`datastruct/json/`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| J-1 | P0 | JSON.ARRLEN | 被 jsonpath.go 双重注册覆盖，prepare=nil → 事务/集群不加锁 | 删除 jsonpath.go 的重复注册，统一一份 |
| J-2 | P0 | JSON.NUMMULTBY | **占位**：恒返回 OK 不改数据 | 实现真实乘法 |
| J-3 | P0 | JSON.STRLEN | **占位**：恒返回 0 | 实现 |
| J-4 | P1 | JSON.MGET | **缺失** | 实现 |
| J-5 | P1 | JSON.ARRINSERT | **缺失** | 实现 |
| J-6 | P1 | JSON.FORGET | **缺失**（DEL 别名） | 注册别名 |
| J-7 | P1 | JSON.CLEAR | **缺失** | 实现 |
| J-8 | P1 | JSON.TOGGLE | **缺失** | 实现 |
| J-9 | P1 | JSON.MERGE | **缺失** | 实现 |
| J-10 | P1 | JSON.MSET | **缺失**（Redis 8.0） | 实现 |
| J-11 | P1 | JSON.RESP | **缺失** | 实现 |
| J-12 | P1 | JSON.DEBUG | **缺失**（MEMORY/FIELDS） | 实现 |
| J-13 | P1 | JSON.GET | ~~execJSONPath 死代码~~ → **execJSONGet 已支持格式选项**；死桩已删（M2at） | ✅ |
| J-14 | P1 | jsonpath.go 全部命令 | prepare=nil，firstKey=0 → 事务/集群锁不全 | 补 prepare 函数 |
| J-15 | P2 | JSON.GET 多路径 | 返回 Go map 序列化，顺序不确定 | 用有序结构 |
| J-16 | P2 | 路径语法 | ~~未兼容 legacy~~ → 遍历已通；**`$` 路径 GET/TYPE 数组封装**（M2aw） | ✅ |

---

### 4.14 Vector Set 命令（命名与语法完全偏离）

涉及文件：`database/vector.go`

> **这是与 Redis 8.0 偏离最严重的模块**。注册名、语法、选项、返回格式全部不兼容。客户端用官方 `VADD`/`VSIM` 调用会得到 `ERR unknown command`。

| # | 优先级 | 问题 | godis 现状 | Redis 8.x | 改进方案 |
|---|--------|------|-----------|-----------|----------|
| V-1 | P0 | 命令命名 | `VSAdd/VSGet/VSDel/VSSearch/VSQuery/VSRange/VSLen/VSCard` | `VADD/VCARD/VDIM/VEMB/VGETATTR/VINFO/VISMEMBER/VLINKS/VRANDMEMBER/VRANGE/VREM/VSETATTR/VSIM` | **全部重命名**；保留旧名作别名（过渡期） |
| V-2 | P0 | VADD 语法 | `VSADD key id vector [METADATA k v...]` | `VADD key [REDUCE dim] (FP32\|VALUES num) vector element [CAS] [NOQUANT\|Q8\|BIN] [EF] [SETATTR] [M]` | 重写 |
| V-3 | P0 | VSIM 选项 | ~~无~~ → **WITHSCORES/WITHATTRIBS/COUNT**；**FILTER `.field==val`**；EPSILON/EF 语法接受 | ✅ M2ad |
| V-4 | P0 | VDIM/VEMB/VINFO/VLINKS/VRANDMEMBER/… | ~~全部缺失~~ → **已有 VADD/VSIM/VREM/VCARD/VDIM/VEMB/VINFO/VISMEMBER/VRANDMEMBER/VSETATTR/VGETATTR/VLINKS(空图)/VRANGE** | ✅ M2w（真 HNSW 延期） |
| V-5 | P1 | 底层算法 | 需确认是否 HNSW（看代码疑似暴力/SQLite） | Redis 用 HNSW + 量化（Q8/BIN） | 实现 HNSW |
| V-6 | P1 | 属性（ATTR） | 用 METADATA，但无读写命令 | 用 VSETATTR/VGETATTR + VSIM FILTER | 对齐 |
| V-7 | P1 | 返回格式 | `[[id, score, vector-bytes], ...]` | WITHSCORES 时 `[member, score, ...]` | 对齐 |

---

### 4.15 TimeSeries 命令

涉及文件：`database/timeseries.go`、`database/timeseries_multi.go`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| TS-1 | P1 | TS.MADD | **缺失** | 实现 |
| TS-2 | P1 | TS.MREVRANGE | **缺失** | 实现 |
| TS-3 | P0 | TS.DECRBY | AOF 写 `ts.incrby`（**回放时 DecrBy 被当 IncrBy**） | AOF 区分 incrby/decrby |
| TS-4 | P1 | TS.CREATE/ADD | ~~策略解析后丢弃~~ → **DuplicatePolicy 持久化 + BLOCK/FIRST/LAST/MIN/MAX/SUM** | ✅ M2ad |
| TS-5 | P1 | 聚合 | ~~无 twa~~ → **时间加权平均 twa** | ✅ M2ac |
| TS-6 | P1 | FILTER 语法 | ~~存在性语义错误 + 遍历类型断言失败~~ → **`label=`/`label!=` + DataEntity 解包** | ✅ M2aa |
| TS-7 | P2 | TS.INCRBY/DECRBY | ~~忽略 RETENTION/LABELS~~ → **auto-create 时接入** | ✅ M2ag |
| TS-8 | P2 | TS.MRANGE | 遍历 db.data 全表 O(N) | 建标签索引 |

---

### 4.16 概率数据结构

涉及文件：`database/probabilistic.go`、`database/tdigest.go`、`database/probabilistic_persist.go`

#### Bloom Filter

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| BF-1 | P0 | BF.LOADCHUNK | 反序列化**恒新建默认容量**，数据丢失 | 实现真实反序列化 |
| BF-2 | P0 | BF.MADD | **恒返全 1**（不区分新增 vs 已存在） | 返回真实结果数组 |
| BF-3 | P1 | BF.RESERVE | 不支持 EXPANSION/NONSCALING | 补全 |
| BF-4 | P1 | BF.MEXISTS/INSERT/CARD | **缺失** | 实现 |

#### Cuckoo Filter

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| CF-1 | P0 | CF.COUNT | **退化为 Exists**（只返 0/1，非真实计数） | 实现真实计数 |
| CF-2 | P0 | CF.LOADCHUNK | 同 BF.LOADCHUNK 丢数据 | 实现真实反序列化 |
| CF-3 | P1 | CF.RESERVE | BUCKETSIZE/MAXITERATIONS/EXPANSION 全忽略 | 接入 |
| CF-4 | P1 | CF.INSERT/INSERTNX/MEXISTS/COMPACT | **缺失** | 实现 |

#### Count-Min Sketch

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| CM-1 | P0 | CMS.INCRBY | 返回 **OkReply** 而非计数值数组 | 返回 `[count1, count2, ...]` |
| CM-2 | P1 | CMS.INITBYPROB/MERGE/INFO | **缺失** | 实现 |

#### Top-K

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| TK-1 | P0 | TOPK.ADD | **恒返空数组**（被 drop 项未返回） | 返回被挤出的项（或 nil） |
| TK-2 | P1 | TOPK.RESERVE | 只读 k，width/depth/decay 忽略 | 接入 |
| TK-3 | P1 | TOPK.COUNT/INCRBY/INFO | **缺失** | 实现 |

#### T-Digest

涉及文件：`database/tdigest.go`（注册名 `TDigest.*`，wire 名 `TDIGEST.*` 与 RedisBloom 一致 ✓）

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| TD-1 | P1 | 多个 | **缺失**：MIN/MAX/RANK/REVRANK/BYRANK/BYREVRANK/RESET/MERGE/TRIMMED_MEAN | 逐个实现 |
| TD-2 | P2 | TDIGEST.ADD | weight 恒 1（Redis 8.0 支持 VALUES+WEIGHTS） | 支持 weight |
| TD-3 | P2 | 互通性 | 自研实现，与 RedisBloom 不互通 | 文档标注 |

---

### 4.17 RediSearch 命令

涉及文件：`database/redisearch.go`、`redisearch_synonym.go`、`redisearch_spellcheck.go`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| RS-1 | P0 | FT.SYNADD | **缺失**（只实现了 SYNUPDATE/SYNDUMP） | 实现（或文档说明用 SYNUPDATE 替代） |
| RS-2 | P0 | FT.SUGDEL/SUGLEN | **占位**（SUGDEL 恒返 1 不删；SUGLEN 恒返 0） | 实现 |
| RS-3 | P1 | FT.ALIASADD/ALIASDEL/ALIASUPDATE | **缺失** | 实现 |
| RS-4 | P1 | FT.ALTER | **缺失**（SCHEMA ADD/CHANGEFIELD） | 实现 |
| RS-5 | P1 | FT.EXPLAIN/EXPLAINCLI | **缺失** | 实现 |
| RS-6 | P1 | FT.PROFILE | ~~缺失~~ → **最小实现**（SEARCH/AGGREGATE + 耗时） | ✅ M2r |
| RS-7 | P1 | FT.CONFIG | ~~缺失~~ → **已实现**（GET/SET 常用项） | ✅ |
| RS-8 | P1 | FT.TAGVALS | **缺失** | 实现 |
| RS-9 | P1 | FT.SEARCH | 不支持**短语**（`"..."`）/SLOP/INORDER/DIALECT；VERBATIM/NOSTOPWORDS/WITHSORTKEYS 未生效 | 补全 |
| RS-10 | P1 | FT.SYNUPDATE | **不按 index 隔离**（`_ = indexName`） | 按 index 维护独立 synonymDB |
| RS-11 | P2 | FT.CREATE | ~~ON HASH/JSON 丢弃~~ → **记录 onType**；HASH 走 HSET 自动索引，JSON 不吃 HSET | ✅ M2aj |
| RS-12 | P2 | FT.ADD | NOSAVE/LANGUAGE 解析后未使用 | 接入或移除 |

---

### 4.18 事务

涉及文件：`database/transaction.go`、`database/database.go`

| # | 优先级 | 问题 | 现状 | Redis 行为 | 改进方案 |
|---|--------|------|------|-----------|----------|
| TX-1 | P0 | EXEC 运行时错误处理 | **中断执行 + 回滚已执行命令**，整体 EXECABORT | **继续执行**，错误作为数组元素返回；Redis 不回滚（无原子性保证，除 discard 外） | 重写 execMulti：遇错放入数组元素，继续下一条；移除自动回滚 |
| TX-2 | P0 | WATCH 后键被修改返回值 | 空数组 `*0` | nil 多重批量 `*-1`（RESP2）/ `_`（RESP3） | 改为 NullBulkReply |
| TX-3 | P1 | UNWATCH | **缺失** | 存在 | 注册 |
| TX-4 | P1 | MULTI 内 WATCH | 不报错（分发顺序导致） | 报 `ERR WATCH inside MULTI is not allowed` | 调整分发顺序 |

> **TX-1 说明**：Redis 的事务**没有原子性回滚语义**（官方文档明确：命令错误会继续执行其余命令）。godis 的"回滚"是自创行为，看似更"安全"，实则与所有依赖 Redis 事务语义的客户端不兼容。

---

### 4.19 发布订阅

涉及文件：`pubsub/pubsub.go`、`pubsub/sharded.go`、`database/pubsub_cmd.go`、`database/sharded_pubsub.go`

| # | 优先级 | 问题 | 现状 | Redis 行为 | 改进方案 |
|---|--------|------|------|-----------|----------|
| PS-1 | P0 | PUBSUB CHANNELS/NUMSUB | **恒空/0**（不遍历真实订阅） | 返回真实数据 | 遍历 hub.subs |
| PS-2 | P0 | PSUBSCRIBE/PUNSUBSCRIBE | ~~缺失~~ → **已实现**（`pubsub` + `pmessage`） | 存在 | ✅ |
| PS-3 | P1 | SUBSCRIBE 状态命令白名单 | ~~无限制~~ → **已限制**（`server.Exec`） | 只允许 (P)SUBSCRIBE/(P)UNSUBSCRIBE/PING/QUIT/RESET | ✅ M2r |
| PS-4 | P1 | UNSUBSCRIBE 无参固定字节 | `$-1\n:0\r\n`（`\n` 非 `\r\n`） | `$-1\r\n:0\r\n` | 修正 |
| PS-5 | P1 | SSUBSCRIBE/SUNSUBSCRIBE/SPUBLISH | ~~传 nil 连接~~ → **真连接 + 确认消息 + SCHANnels**；槽位仍为简化哈希（CRC16 延期） | ✅ M2v（CRC16 槽仍缺） |
| PS-6 | P2 | PUBLISH 返回值 | 当前订阅者数（OK） | — | — |

---

### 4.20 脚本与函数

涉及文件：`database/scripting.go`、`scripting/`、`database/functions.go`、`functions/engine.go`

#### 脚本

| # | 优先级 | 问题 | 现状 | Redis 行为 | 改进方案 |
|---|--------|------|------|-----------|----------|
| SC-1 | P0 | EVAL 不缓存脚本 | EVAL 后 EVALSHA 报 NOSCRIPT | EVAL 自动缓存脚本 | EVAL 执行时 SaveScript |
| SC-2 | P0 | redis.pcall 返回 `{err=}/{ok=}` 表 | 与 Redis 直接返回值语义不同 | pcall 出错时返回带 err 字段的表，成功直接返回值 | 对齐 |
| SC-3 | P1 | cjson/cmsgpack/bit 库 | ~~不支持~~ → **cjson+cmsgpack+bit（含 tohex/rol/ror/bswap）** | ✅ M2ar–M2av |
| SC-4 | P1 | 脚本复制 | 效果级（每写命令各自进 AOF） | Redis 7+ 默认 verbatim（复制脚本本身） | 增加 verbatim 模式开关 |
| SC-5 | P1 | EVAL/EVALSHA 在 MULTI | ~~prepare=nil~~ → **prepareEval + redis.call 走 execWithLock** | ✅ M2v |
| SC-6 | P2 | PING 带 message | server 路径返回 status `+msg` | bulk `$<len>\r\nmsg\r\n` | 改 BulkReply |

#### 函数

| # | 优先级 | 问题 | 现状 | Redis 行为 | 改进方案 |
|---|--------|------|------|-----------|----------|
| FN-1 | P0 | FCALL 执行引擎 | ~~legacy LuaEngine~~ → **gopher-lua（`scripting.Engine`）** | ✅ M2z |
| FN-2 | P1 | API 版本检查 | 不检查 shebang `api_version` | 检查 | 校验 |
| FN-3 | P1 | FUNCTION DUMP 格式 | ~~纯文本拼接~~ → **Godis `GODISFN1` 二进制 envelope**（RESTORE 兼旧文本） | ✅ M2ag（非 Redis wire） |
| FN-4 | P1 | Library.Engine | ~~硬编码 `LUA`~~ → **从 `#!lua` shebang 解析** | ✅ M2aa |

---

### 4.21 连接与服务器命令

涉及文件：`database/connection.go`、`client_conn.go`、`systemcmd.go`、`memory.go`、`monitor.go`、`time_cmd.go`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| SV-1 | P1 | SHUTDOWN | ~~缺失~~ → **已实现**（选项接受 + hook） | ✅ |
| SV-2 | P1 | LASTSAVE | **缺失** | 实现 |
| SV-3 | P1 | LOLWUT | **缺失** | 实现（彩蛋） |
| SV-4 | P1 | FAILOVER | ~~缺失~~ → **最小子集**（ABORT/FORCE/TO/TIMEOUT 解析；无副本时报错） | ✅ M2s（完整协调切换仍缺） |
| SV-5 | P1 | RESET | **缺失** | 实现（重置连接状态） |
| SV-6 | P1 | REPLICAOF | **缺失**（只有 SLAVEOF，但大小写不敏感所以 `REPLICAOF` 能匹配命令名；语义上 OK） | 注册别名明确 |
| SV-7 | P1 | CLIENT LIST | 恒返空 bulk | 返回真实客户端列表 |
| SV-8 | P1 | CLIENT INFO | 硬编码字符串 | 返回真实信息 |
| SV-9 | P1 | CLIENT KILL | 恒返 0（不解析过滤条件） | 实现 ID/TYPE/USER/ADDR/LADDR/SKIPME 过滤 |
| SV-10 | P1 | CLIENT NO-EVICT/SETINFO | **缺失** | 实现 |
| SV-11 | P1 | CLIENT TRACKING OPTIN/NOLOOP | ~~未挂钩~~ → **OPTIN↔CACHING + NOLOOP 跳过自失效** | ✅ M2t |
| SV-12 | P1 | MEMORY USAGE | ~~恒返 0~~ → 粗估；**含 ExpireDict/hash/TS/vector**；**SAMPLES 采样估算**（默认 5，0=全量） | ✅ M2ag/M2ai |
| SV-13 | P1 | MONITOR | ~~仅 OK~~ → **流式广播**（`BroadcastMonitor`） | ✅ M2r |
| SV-14 | P1 | FLUSHDB | 不支持 ASYNC/SYNC（带参数 arity 报错） | 支持 |
| SV-15 | P1 | HELLO/INFO 版本不一致 | HELLO 报 8.0.0，INFO 报 1.2.8 | 统一 |
| SV-16 | P2 | CLIENT TRACKING RESP2 | ~~直接拒绝~~ → **RESP2+REDIRECT 可用**；GETREDIR；redirect 经 client registry | ✅ M2ah |

---

### 4.22 ACL

涉及文件：`database/acl.go`、`acl/acl.go`

| # | 优先级 | 问题 | 现状 | Redis 行为 | 改进方案 |
|---|--------|------|------|-----------|----------|
| AC-1 | P0 | ACL GENPASS | **伪随机固定序列**（`chars[i%62]`） | 密码学安全随机 | 用 `crypto/rand` |
| AC-2 | P1 | ACL WHOAMI | 恒返 `default`（不看连接） | 返回当前连接用户 | 查 conn.user |
| AC-3 | P1 | ACL LIST | 不输出 `~key` 模式 | 输出全部规则 | 补全 |
| AC-4 | P1 | `%R~/%W~/%RW~` & `&channel` & `(...)` | ~~需确认~~ → **已支持**（含 clearselectors） | ✅ M2ar/M2as |
| AC-5 | P2 | NOPERM 错误格式 | `NOPERM User <name> has no permissions to run the '<CMD>' command` | 与 Redis 一致（OK） | — |

---

### 4.23 持久化（AOF / RDB）

涉及文件：`aof/aof.go`、`aof/rewrite.go`、`aof/rdb.go`、`database/persistence.go`

| # | 优先级 | 问题 | 现状 | 改进方案 |
|---|--------|------|------|----------|
| PE-1 | P0 | RDB 不写新类型 | stream/JSON/bitmap/vector/timeseries/概率结构 全部丢失 | 为每个新类型实现 RDB 编码（或转译为 RESP 命令 preamble） |
| PE-2 | P0 | DUMP/RESTORE 格式 | 自研 5 字节 header，仅 String | 用标准 RDB payload + 版本 + CRC64 |
| PE-3 | P1 | RDB aux redis-ver | 硬编码 `6.0.0` | 改为真实版本（`8.0.0`） |
| PE-4 | P1 | 无 RDB 自动快照 | ~~配置桩~~ → **dirty 计数 + 每秒 checkSavePoints→BGSAVE**；`rdb_changes_since_last_save` 接 dirty | ✅ M2ae |
| PE-5 | P1 | hash field TTL 降级 | ~~ExpireDict 降级普通 hash~~ → **opaque `hexpire` 编码**（RDB/DUMP/RESTORE 保留 field TTL） | ✅ M2af |
| PE-6 | P2 | AOF rewrite 模式 | RESP 与 RDB preamble 双模式（OK） | — |
| PE-7 | P2 | aof-use-rdb-preamble | 默认 true（OK） | — |

---

### 4.24 复制

涉及文件：`database/replication_master.go`、`replication_slave.go`

| # | 优先级 | 问题 | 现状 | 改进方案 |
|---|--------|------|------|----------|
| RP-1 | P1 | WAIT 命令 | **缺失** | 实现（见 K-4） |
| RP-2 | P1 | REPLCONF 能力协商 | ~~仅 ack~~ → **记录 listening-port / ip-address / capa** | ✅ M2ab |
| RP-3 | P1 | backlog 无环形 | ~~无限增长 + 10MB 重写~~ → **固定容量环形缓冲**（满则丢弃最旧） | ✅ M2ae |
| RP-4 | P1 | master 不发 REPLCONF GETACK | masterCron 只发 PING | 实现 GETACK 检测 slave 存活 |
| RP-5 | P1 | RDB aux redis-ver=6.0.0 | 真 redis slave 误判主库版本 | 改 8.x |
| RP-6 | P3 | 与真 redis 互主从 | 简单类型可行；新类型丢失 | 长期目标 |

---

### 4.25 集群

涉及文件：`cluster/`、`cluster/core/`、`cluster/commands/`、`cluster/raft/`

> **集群模块与官方 Redis Cluster 协议级根本不兼容**。当前实现是"自研基于 Raft 的集群"，槽位、总线协议、重定向全部不同。

| # | 优先级 | 问题 | 现状 | Redis 行为 | 改进方案 |
|---|--------|------|------|-----------|----------|
| CL-1 | P0 | 槽位算法 | **1024 槽 + CRC32** | 16384 槽 + CRC16 | 改为 CRC16 % 16384 |
| CL-2 | P0 | MOVED/ASK 重定向 | **未实现**（服务端透明 Relay） | 客户端收 MOVED/ASK 自行重定向 | 实现重定向（或明确声明"代理模式"） |
| CL-3 | P0 | CLUSTER NODES/INFO/SLOTS | **硬编码**（地址 127.0.0.1:6379，slot 0-16383 与实际 1024 不符） | 真实集群状态 | 从 Raft FSM 读真实状态 |
| CL-4 | P0 | CLUSTER MEET/ADDSLOTS/DELSLOTS/SETSLOT 等 | **未实现** | 存在 | 实现（或保留自研加入流程） |
| CL-5 | P0 | cluster bus 协议 | 自研 RESP RPC | 官方 gossip 二进制协议 | 二选一：兼容官方 / 明确声明自研 |
| CL-6 | P0 | `redis-cli --cluster` 兼容 | **不可行**（上述全部不兼容） | 可行 | 解决 CL-1~CL-5 后可用 |
| CL-7 | P1 | CLUSTER MYID/REPLICAS/SLAVES/FAILOVER/FORGET/RESET/SHARDS/KEYSLOT | 大部分缺失 | 存在 | 逐个实现 |
| CL-8 | P2 | 跨节点 TCC | 自研两阶段（OK，但与 Redis 不同） | Redis 单槽原子 + 多槽不支持 | 可接受 |

> **决策点**：CL-1~CL-6 需要明确产品方向——是要兼容官方 Redis Cluster 协议（工作量巨大），还是继续自研（需重命名、避免误导）。建议在计划评审时拍板。

---

### 4.26 配置

涉及文件：`config/config.go`、`database/config_cmd.go`

| # | 优先级 | 问题 | 现状 | 改进方案 |
|---|--------|------|------|----------|
| CF-1 | P0 | 配置解析器分隔符 | ~~单空格、无引号~~ → **多空白 + 引号值** | ✅ M2aa |
| CF-2 | P1 | 布尔值 | ~~仅 yes~~ → **yes/no/on/off/true/false/1/0**（`ParseConfigBool`） | ✅ M2t |
| CF-3 | P1 | 缺失指令 | ~~大量缺失~~ → **已补常用桩**：timeout/tcp-keepalive/loglevel/logfile/protected-mode/daemonize/pidfile/lazyfree-lazy-eviction/proto-max-bulk-len（语义多为存取） | ✅ M2u |
| CF-4 | P1 | maxmemory/maxmemory-policy | ~~无 eviction~~ → **写路径闭环**：noeviction→OOM；allkeys-*/volatile-* 可淘汰（键数估算内存） | ✅ M2u |
| CF-5 | P1 | CONFIG SET 范围 | ~~仅少数键~~ → **含 appendonly/appendfsync/maxclients/maxmemory** | ✅ M2t |
| CF-6 | P2 | CONFIG RESETSTAT/REWRITE | ~~空操作~~ → **RESETSTAT**（M2af）+ **REWRITE 写回配置文件** | ✅ M2ag |

---

### 4.27 SORT 与其他通用命令

涉及文件：`database/sort.go`

| # | 优先级 | 命令 | 问题 | 改进方案 |
|---|--------|------|------|----------|
| SO-1 | P0 | SORT | **占位**：DESC 不生效、ALPHA 不生效、BY pattern 不生效、**无真正排序步骤**（elements 按原序）；GET 找不到值返回 `[]byte{}` 而非 nil | 完整重写：实现 sort.Slice + BY/GET/ALPHA/DESC/LIMIT/STORE 全子句 |

---

## 五、实施路线与里程碑

### M1：稳定性与正确性（P0 修复，建议 2-3 周）

目标：消除所有 panic、数据损坏、协议错误。

- 崩溃类修复：负 offset/count 校验（P0-1~P0-6）、空字符串边界
- 数据损坏修复：NaN 拒绝（P0-7）、空容器删键（P0-8/9）、GEOSEARCHSTORE score（P0-10）、RENAME src==dest（P0-11）、XTRIM 假条目（P0-12）、JSON 占位实现（P0-13/14）
- 协议修复：ZMPOP 嵌套编码（P0-15）、RESP2 降级（P0-16、P-1）
- 事务语义修正：EXEC 继续执行（P0-17）、WATCH 返回 nil（P0-18）
- 阻塞命令真实实现：BLPOP/BZPOP* 系列（P0-19/20）—— 可先用"等待队列 + channel 唤醒"方案
- ACL GENPASS 安全随机（P0-29）
- 位序 MSB-first（B-1）—— **数据迁移注意**：修复后已有位图数据需重新生成

### M2：核心命令补全与错误串规范（高优 P1，建议 3-4 周）

- 错误串全量规范化（E-1~E-11）
- String：SET 全选项（EXAT/PXAT/KEEPTTL/GET）、溢出检查（S-1~S-9）
- Keys：EXPIRE NX/XX/GT/LT、WAIT、DUMP/RESTORE 标准格式（K-1~K-7）
- List：LPOS、BRPOPLPUSH、阻塞修正（L-4~L-7）
- Hash：HEXPIRETIME、FIELDS numfields 语法、HINCRBY 整数回复（H-1~H-10）
- Set：SMOVE、SPOP/SRANDMEMBER 单值返回、SINTERCARD（SE-1~SE-6）
- ZSet：ZADD 全选项、ZRANGE 统一语法、ZRANGESTORE/ZRANDMEMBER（Z-1~Z-9）
- Bitmap：BITOP、BITFIELD（B-4/5）

### M3：新类型模块补全（P1，建议 4-6 周，可并行）

- Stream：XINFO 全套、XCLAIM/XAUTOCLAIM/XSETID、XDELEX/XACKDEL（ST-1~ST-13）
- JSON：MGET/ARRINSERT/CLEAR/TOGGLE/MERGE/MSET/RESP/DEBUG、INDENT 选项（J-4~J-15）
- Vector Set：**命名与语法全面对齐 VADD/VSIM**（V-1~V-7）—— 工作量大，建议独立子项目
- TimeSeries：MADD/MREVRANGE、twa、AOF 修正（TS-1~TS-8）
- 概率结构：补全各模块缺失子命令、修正占位实现（BF/CF/CM/TK/TD 系列）
- RediSearch：SYNADD、ALIAS*、ALTER、EXPLAIN、PROFILE、短语/SLOP/DIALECT（RS-1~RS-12）

### M4：持久化与复制增强（P1，建议 2-3 周）

- RDB 新类型编码（PE-1）
- RDB aux 版本修正（PE-3）
- save 点位自动触发快照（PE-4）→ ✅ M2ae
- hash field TTL 持久化（PE-5）→ ✅ M2af
- CONFIG REWRITE 写回配置文件（CF-6 余）→ ✅ M2ag
- FUNCTION DUMP 二进制（FN-3）→ ✅ M2ag（Godis 内部格式）
- WAIT 命令（RP-1）
- REPLCONF 能力协商（RP-2）

### M5：连接/服务器/ACL 完善（P1-P2，建议 2 周）

- SHUTDOWN/LASTSAVE/RESET/FAILOVER（SV-1~SV-5）
- CLIENT LIST/INFO/KILL 真实实现（SV-7~SV-10）
- MEMORY USAGE（SV-12）
- MONITOR 流式（SV-13）
- ACL WHOAMI 查连接、LIST 补全（AC-2~AC-4）

### M6：集群方向决策与实施（P0/P1，建议评审后单独排期）

- **先评审**：兼容官方 Redis Cluster vs 继续自研（CL-1~CL-6 决策点）
- 若兼容：槽位改 CRC16/16384、实现 MOVED/ASK、实现 CLUSTER MEET/ADDSLOTS 等、实现 gossip bus
- 若自研：重命名为 "Godis Cluster"，明确声明不兼容官方，避免误导

### M7：配置解析与 eviction（P0/P1，建议 1-2 周）

- 配置解析器重写（CF-1/2）
- 补全常用指令（CF-3）
- maxmemory eviction 实现（CF-4）
- CONFIG SET 扩展（CF-5）

### M8：兼容性测试套件建立（贯穿全程，建议立即启动）

- 引入差异化测试：同一命令对 godis 与真实 redis 8.x 跑，对比输出
- 移植部分 Redis TCL 测试套件
- 见 [六、验证策略](#六验证策略)

---

## 六、验证策略

### 6.1 自动化兼容性测试

建议建立 `tests/compat/` 目录，包含：

1. **命令级差异测试**：用 Go 测试，对每个命令构造输入，分别调 godis 和真实 redis（通过 docker/dockertest），断言：
   - 错误串完全一致（或可接受差异白名单）
   - 回复类型一致（Int/Bulk/Array/Map...）
   - 回复内容一致
2. **模糊测试（fuzzing）**：对边界输入（负数、空、超大、NaN、Unicode）做 fuzz，找出 panic 路径
3. **协议级测试**：用 RESP2/RESP3 客户端分别连接，验证降级正确

### 6.2 客户端库互通测试

用主流客户端库（redis-py、jedis、go-redis、ioredis、lettuce、node-redis）跑其测试套件或样例程序，确认能正常工作。

### 6.3 redis-cli 回归

维护一个 `compat-commands.txt`，记录每条命令的标准调用与期望输出，CI 中用真实 redis-cli 跑。

### 6.4 复制/集群互通测试

- 用真实 redis-server 8.x 作 master、godis 作 slave，验证字符串/列表等基础类型复制
- 反向验证
- 集群：待 M6 决策后定

### 6.5 持久化往返测试

- 写入各种类型数据 → SAVE → 重启 → 验证数据完整
- AOF rewrite 前后数据一致
- DUMP/RESTORE 与真实 redis 互通

---

## 附录 A：缺失 / 已补命令总表

> 初评时「完全未实现」列表；**2026-07-24 修订**：多数已落地。仅列仍缺或明显不完整项。

### 仍缺或明显不完整
- FAILOVER 完整主从协调切换（最小命令已有）
- 集群 CLUSTER MEET/ADDSLOTS/… 与官方协议（**延期**）
- Vector 官方名 VADD/VSIM 全面替换（现双名）
- HyperLogLog 与 Redis 编码互通（**延期**）
- 精确 Redis 级 used_memory 会计 / 真 LFU clock（现为键数估算 + 访问计数）
- FUNCTION DUMP 与 Redis 官方二进制互通（现为 Godis `GODISFN1`）

### M2r–M2aq 已落地（勿再当缺口）
含 MONITOR、CLIENT UNBLOCK、XREAD 真阻塞、配置布尔/SET 扩展、XAUTOCLAIM MINID、TRACKING OPTIN/NOLOOP、**maxmemory eviction 闭环**、CF-3 常用配置桩、OBJECT IDLETIME/FREQ、**SSUBSCRIBE 真连接/SCHANNELS**、**EVAL prepare**、**VRANDMEMBER**、save/tcp-backlog、**VSETATTR/VGETATTR/VLINKS/VRANGE**、**timeout 踢线**、**REPLCONF GETACK**、**CLIENT NO-TOUCH/REPLY**、**FCALL execWithLock**、**COPY 深拷贝**、**VSIM WITHATTRIBS**、**XSETID MAXDELETEDID**、**HGETEX/HSETEX/HGETDEL FIELDS**、**ACL DRYRUN key**、**PFCOUNT 多键锁**、**CLIENT age/idle**、**CLIENT INFO 对齐 LIST**、**XDELEX/XACKDEL 逐 ID**、**JSON.SET/DEL undo**、**FCALL→gopher-lua**、**协议错关连接**、**FUNCTION Engine shebang**、**TS FILTER 存在性**、**配置引号解析**、**GEOSEARCH 含自身**、**GEOHASH 缺键 null 数组**、**UnknownErr 文案**、**XREADGROUP DeliveryCount**、**REPLCONF capa**、**SINTERCARD LIMIT 早停**、**GEORADIUS 选项**、**TS twa**、**TS DUPLICATE_POLICY**、**XGROUP SETID ENTRIESREAD**、**VSIM FILTER**、**save 点位自动 BGSAVE**、**52-bit geohash**、**环形 repl backlog**、**hash field TTL opaque RDB/DUMP**、**CONFIG RESETSTAT/REWRITE**、**XPENDING TIME**、**MEMORY USAGE ExpireDict**、**FUNCTION DUMP GODISFN1**、**TS.INCRBY RETENTION/LABELS**、**TRACKING RESP2+REDIRECT/GETREDIR**、**RESTORE IDLETIME/FREQ**、**PING Bulk**、**OBJECT bloomfilter**、**MEMORY USAGE SAMPLES**、**LCS 缺键空串**、**Protocol error 双引号**、**SRANDMEMBER 随机采样**、**DUMP opaque stream/JSON/vector/TS**、**FT.CREATE ON HASH|JSON**、**CLIENT LIST TYPE**、**CLIENT PAUSE 执行路径**、**INFO ops/sec/pubsub**、**FLUSH SYNC|ASYNC 校验**、**CLIENT KILL TYPE/USER**、**HELLO 真实 id**、**ROLE**、**DUMP Bloom/HLL opaque**、**CLIENT KILL LADDR/MAXAGE**、**HELLO role**、**DUMP Cuckoo opaque**、**INFO sync_***、**DUMP CMS/TopK/TDigest opaque**、**blocked_clients+ZSet**、**OBJECT HLL/vector**、**XINFO FULL COUNT**、**FT.SEARCH 选项校验**、**pubsub RESP3 null**、**MEMORY HELP**、**CF EXPANSION**、**WAIT GETACK**、**TDIGEST WEIGHTS**、**FT.ADD NOSAVE**。

### 已实现（原附录误标为缺失，勿再扫）
WAIT、UNWATCH、LASTSAVE、SHUTDOWN、LOLWUT、RESET、PSUBSCRIBE/PUNSUBSCRIBE、BRPOPLPUSH、LPOS、SMOVE、ZRANGESTORE、ZRANDMEMBER、BITOP、BITFIELD、PFDEBUG（GETREG/DECODE/PERIOD）、HEXPIRETIME/HPEXPIRETIME、XCLAIM、XAUTOCLAIM（含 MINID）、XINFO 子集、JSON 多项、BF/CF/CMS/TOPK/TDIGEST 多项、FT.SYN*/ALIAS*/ALTER/EXPLAIN/CONFIG/TAGVALS/PROFILE 等。

### Stream / 模块（部分仍缺选项）
- XDELEX/XACKDEL 逐 ID 状态已对齐；其余 Stream 选项视代码为准
- JSON MERGE/MSET 等边缘命令可能仍缺
---

## 附录 B：错误串差异对照表

| godis | Redis 8.x | 出现命令 |
|-------|-----------|----------|
| `Err syntax error` | `ERR syntax error` | 几乎所有命令 |
| `Err unknown` / `ERR unknown` | `ERR unknown error`（panic 兜底） | UnknownErrReply |
| `no such key` | `ERR no such key` | RENAME |
| `ERR invalid expire time in setex` | `ERR invalid expire time in 'setex' command` | SETEX/PSETEX |
| `please enable aof before using save` | （无限制） | SAVE/BGSAVE |
| `AppendOnly is false, you can't rewrite aof file` | （无限制） | BGREWRITEAOF |
| `strconv.ParseInt: parsing "x": invalid syntax` | `ERR value is not an integer or out of range` | SETRANGE |
| `Invalid section for 'info' command` | `ERR Invalid section for 'info' command` | INFO |
| `Unknown subcommand '<sub>'` | `ERR Unknown subcommand or wrong number of arguments for '<sub>'` | COMMAND |
| `ERR timeout is not a float or out of range` | （一致） | BLPOP 等 |
| `ERR value is not a valid float`（BZMPOP timeout） | `ERR timeout is not a float or out of range` | BZMPOP |
| `Unknown subcommand or wrong number of arguments for  '<sub>'`（两空格） | `... for '<sub>'` | SLOWLOG |

---

## 评审清单（建议）

在动手前，请就以下决策点与团队对齐：

1. **集群方向**（CL-1~CL-6）：兼容官方 vs 自研重命名？
2. **Vector Set**（V-1~V-7）：是改名兼容（破坏现有调用方）还是双名共存？
3. **位图位序**（B-1）：修复会破坏现有位图数据，是否提供迁移工具？
4. **HyperLogLog**（HLL-1）：是否值得对齐 Redis 编码以实现互通，还是声明"不互通"？
5. **事务回滚**（TX-1）：移除自动回滚会改变现有依赖该行为的客户端行为，是否保留兼容模式？
6. **阻塞命令**（P0-19/20）：用 channel 唤醒方案是否可接受，还是需要更精细的事件驱动？

---

*文档结束。如需针对某个模块展开更细的实施设计（如 Vector Set 重写方案、集群兼容路径），可另行起草专项设计文档。*
