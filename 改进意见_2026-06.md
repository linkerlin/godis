# Godis 项目系统性审阅报告（2026-06）

> 在既有 [`改进意见.md`](改进意见.md)（2026-04）基础上，结合仓库最新状态（commit b4f50d1）及 **2026-06-20 二次审阅** 做新鲜检查。  
> 本文档为**当前唯一权威改进索引**；`改进意见.md` 保留作历史对照，新立项以本文为准。

---

## 审阅对照说明（对本文档的自我校验）

| 条目 | 结论 | 说明 |
|------|------|------|
| §0 仓库卫生 | ✅ 成立 | `git ls-files` 确认 `godis`/`test.rdb`/`coverage`/raft 测试残留均已入库 |
| §1.1 优雅关闭 TODO | ✅ 成立 | `redis/server/std/server.go:142` 仍存在 |
| §1.4 集群迁移空壳 | ✅ 成立 | `doMigrateSlot` 仅打日志返回 nil |
| §1.5 AOF 并发 rewrite | ✅ 成立 | `aof/rdb.go:19` 有 todo 注释 |
| §1.7 gnet/std 不对称 | ✅ 合理 | 建议对齐，但优先级低于 ACL/迁移 |
| §1.9 `flagAdmin` 未使用 | ⚠️ **需修正** | `flagAdmin` 已在 Script/ACL/Client 等 20+ 处使用；**仅 `flagLoading` 未使用** |
| §3 测试/Fuzz/race | ✅ 成立 | `go test ./...` 全绿；`database` 覆盖率 **33.9%** |
| §4 文档漂移 | ✅ 成立，且比原文更严重 | 见 §4.2 补充 |
| §8「兼容度 ~96–99%」 | ❌ **偏高** | 协议/经典类型覆盖高，**命令级**约 70–85%（见 §4.2） |
| **缺失：ACL 未启动** | 🔴 二次审阅新增 P0 | `InitACLEngine()` 从未在启动路径调用 |
| **缺失：AUTH 绕过** | 🔴 二次审阅新增 P0 | `execAuth` 在无 ACL 时直接 OK |

---

## 0. 仓库卫生（立即可改）

`.git ls-files` 暴露以下**本不该入库**的产物：

| 文件/目录 | 问题 | 建议 |
|----------|------|------|
| `godis` | 已编译二进制 | 加入 `.gitignore`，`git rm --cached godis` |
| `test.rdb` | 测试遗留 | `.gitignore` 已有 `*.rdb` 但未阻止根目录已跟踪文件；从索引移除 |
| `coverage` | 覆盖率产物 | 统一用 `go test -coverprofile=coverage.out`；忽略 `coverage`/`coverage.out` |
| `cluster/core/test/*/raft.db*` | 测试残留 | 加入 `.gitignore`（`cluster/core/test/**`） |
| 根目录 9+ 份过程报告 | 历史文档污染 | 移入 `docs/history/` 或 `docs/reviews/` |
| `.travis.yml` | 早期 CI，已被 GitHub Actions 取代 | 删除或 README 注明废弃 |
| `.vscode/launch.json` | 编辑器配置 | 视团队规范决定 |

**`.gitignore` 建议补全：**

```gitignore
/godis
/godis.exe
/coverage
/cluster/core/test/
```

**端口与常量分裂：**

- `lib/consts/consts.go:23` → `DefaultPort = 6379`（**无引用**，死常量）
- `config/config.go` 默认 `6379`；`main.go:30` 与 `example.conf` 用 **6399**
- 建议：删除 `consts.DefaultPort`，或在文档中明确「6399 为 Godis 惯例，非 Redis 默认」

**Flag 重复定义：**

- `lib/consts/consts.go:49-52` 的 `FlagWrite/FlagReadOnly/FlagSpecial` 与 `database/router.go:32-39` 含义相同但未互通；`consts` 侧**无引用**，应删除或统一到 `router.go`

---

## 1. P0 — 安全与可靠性（最高优先）

> 本节为 **2026-06-20 二次审阅新增**，原 2026-06 稿未覆盖，应置于所有改进项之前。

### 1.1 ACL 引擎写了但未启动

`InitACLEngine()` 定义于 `database/acl.go:113`，但 `newServerWithSize`（`database/server.go:80`）**从未调用**。

后果：

- `aclEngine` 始终为 `nil` → `ACL SETUSER` 等返回 "ACL engine not initialized"
- 正常命令路径**未调用** `user.CheckCommand()`，ACL 权限不生效
- `isAuthenticated()` 仅比对 `requirepass`，与 ACL 用户体系脱节
- `AGENTS.md` 配置示例含 `aclfile`，但 `config/config.go` **无此字段**

**建议：**

1. 在 `newServerWithSize` 末尾调用 `server.InitACLEngine()`
2. 在 `Server.Exec` 命令分发前增加 ACL 权限检查（参考 `ACL DRYRUN` 已有逻辑）
3. 实现 `aclfile` 配置或从文档中移除

### 1.2 AUTH 路径存在认证绕过

`database/connection.go:37-42`：

```go
} else {
    // Check against requirepass config if no ACL
    // Simplified: always allow for now
}
return protocol.MakeOkReply()
```

当 `aclEngine == nil` 时，经 `DB.Exec` 可达的 `execAuth`（如 Lua `redis.call("AUTH")`）**直接返回 OK**。HELLO 中的 AUTH 验证后也未 `SetPassword` 更新连接状态（`:85-93`）。

**建议：** 对齐 Redis 语义；`execAuth` 必须校验 `requirepass`；HELLO AUTH 成功后更新连接认证状态。

### 1.3 集群 Slot 迁移为空实现

`cluster/core/rebalance.go:573-578` — `doMigrateSlot` 仅打日志返回 `nil`，重平衡流程会「成功」但不迁移数据。

### 1.4 panic 治理（继承自 `改进意见.md` P0，仍有效）

- **`datastruct/*`** 内部不变量仍可能 `panic`；命令层校验应确保客户端无法触发
- **Raft FSM**：反序列化失败已改为打日志跳过（`cluster/raft/fsm.go`），运维需监控此类日志

### 1.5 输入校验覆盖不均（继承自 `改进意见.md`，部分已落实）

**已落实：** prepare 键、`ValidateArgsCount`、MULTI/EXEC 路径、SET value、JSON/RediSearch/Lua 热点路径。

**仍缺：** `prepare=nil` 的命令（`redisearch`、`timeseries`、`geo`、`vector`、`probabilistic` 等）键/值大小校验不足；`requirepass` 明文比较无时序安全（`database/systemcmd.go`）。

---

## 2. 代码层未结项问题

### 2.1 `Handler.Close` — `// TODO: concurrent wait`

`redis/server/std/server.go:142`。改进意见 P1-4.2 已列，至今未做。

```go
func (h *Handler) Close() error {
    h.closing.Set(true)
    // TODO: concurrent wait
    h.activeConn.Range(func(key interface{}, val interface{}) bool {
        client := key.(*connection.Connection)
        _ = client.Close()         // 强关连接，正在处理的命令会中断
        return true
    })
    h.db.Close()
    return nil
}
```

**建议：** `sync.WaitGroup` 跟踪 `Handle` goroutine；`Close` 先拒新连接 → `wg.Wait()`（带超时）→ 关 db；`Server.Exec` 入口检查 `closing`。

### 2.2 多 DB 事务未支持

`database/server.go:341` — `// todo: support multi database transaction`。`MULTI/EXEC` 只在单 DB 内原子。

**建议：** 文档明确「跨 DB 事务不支持」；或在 `StartMulti` 检测多 `dbIndex` 时返回明确错误。

### 2.3 `INFO` 命令 21 处 TODO 占位

`database/systemcmd.go` — `master_repl_offset` / `rdb_last_save_time` / `used_cpu_user` / `blocked_clients` / `pubsub_patterns` 等均为 0。

**建议：** 优先补齐**复制与持久化**字段；CPU/阻塞客户端后续对接 `runtime`/`lib/stats`。

### 2.4 AOF 并发 rewrite 无锁

`aof/rdb.go:19` — `// todo: forbid concurrent rewrite`。并发 `BGSAVE`/`BGREWRITEAOF` 存在双写 `dump.rdb` 风险。

### 2.5 `go vet` 警告

`database/stream.go:231, 322` — `stream.StreamID{0, 0}` 应改为 `stream.StreamID{Timestamp: 0, Sequence: 0}`。

### 2.6 gnet 与 std server 不对称

| 项目 | std | gnet | 建议 |
|------|-----|------|------|
| `closing` 拒新连接 | ✅ | ❌ | gnet 补齐 |
| 写错/统计 | 有 `stats.RecordOutput` | `c.Write` 忽略错误 | 对齐 |
| 优雅关闭 | TODO | 仅 `eng.Stop` | 与 std 同改 |
| 解析器 | `ParseStream` 异步 | `ParseV2` 同步 | 长期统一 |

### 2.7 `Server.Exec` 顶层 recover 吞掉错误

`database/server.go:175-181` — panic 后只回 `UnknownErrReply`，难定位。建议记录**命令名 + dbIndex**，或透出受控内部错误文案。同样问题在 `replication_master.go`、`replication_slave.go`（5 处 recover）。

### 2.8 `router.go` Flag 设计

```go
const flagWrite = 0
const (
    flagReadOnly = 1 << iota   // 1
    flagSpecial                 // 2
    flagAdmin                   // 4  ← 已在 Script/ACL/Client 等 20+ 处使用
    flagFast                    // 8  ← 已在 Auth/Ping/Hello 等使用
    flagLoading                 // 16 ← **唯一未使用的死代码**
)
```

`flagWrite = 0` 与位运算风格不一致，但 `isReadOnlyCommand` 只检查 `&flagReadOnly`，当前逻辑合理。**仅需处理 `flagLoading`：** 删除或在 loading 状态下启用。

### 2.9 错误类型两条路径并存

- 部分命令：`protocol.MakeErrReply("...")` 直写文案
- 部分命令：`errs.Is(err, errs.ErrCodeKeyTooLarge)` 区分错误码

**建议：** 统一为 errs 码 + 文案映射，便于客户端解析。

### 2.10 并发安全隐患（二次审阅补充）

| 位置 | 问题 |
|------|------|
| `database/server.go:50-52` | `clientPaused` / `clientPauseEnd` / `clientPauseMode` **无 mutex** |
| `database/scripting.go` | 全局 `scriptEngine` lazy init **无 sync.Once** |
| `redis/server/std/server.go:142` | 关闭与在途 Handle 竞态 |

### 2.11 大文件拆分（可维护性，P1）

| 文件 | 约行数 | 建议 |
|------|--------|------|
| `database/sortedset.go` | 1322 | 按 ZADD/ZRANGE/ZUNION 拆分 |
| `database/redisearch.go` | 987 | CREATE/SEARCH/AGGREGATE 分离 |
| `database/string.go` | 858 | 位操作 vs 字符串命令 |
| `database/stream.go` | 794 | XADD/XREAD vs XGROUP |
| `database/server.go` | 609 | Exec 路由 vs 生命周期 |

拆分原则：**只移动代码，不改行为**，配合测试回归。

### 2.12 代码重复（低优先）

- `isAuthenticated` 在 `database/systemcmd.go` 与 `cluster/core/command.go` 完全相同
- `database/connection.go` 的 `execAuth` 与 `database/systemcmd.go` 的 `Auth` 双实现
- `scripting/engine.go` 与 `functions/engine.go` 结构相似，可考虑统一抽象

---

## 3. 架构层

### 3.1 `config.Properties` 全局可变状态

`main.go:101` 直接赋值；测试也覆盖 `config.Properties`。短期封装只读视图；中期 `Server`/`Handler` 持有 `*config.ServerProperties`（见 `改进意见.md` P1-4.1）。

### 3.2 `cmdStats` 全局可变

`database/cmdstats.go:23` — 测试间共享，难以隔离。建议纳入 `Server` 实例。

### 3.3 `Server.Exec` vs `DB.Exec` 双路径

- `Server.Exec`：含 auth、复制、pubsub、特殊命令路由
- `DB.Exec`：无 auth，供 Lua/集群内部调用

**建议：** 在 `AGENTS.md` 或 `docs/ARCHITECTURE.md` 中明确边界，防止误用。

### 3.4 集群 FSM 错误处理

`改进意见.md` P0-3.1 已记录「反序列化失败打日志跳过」——需运维监控；可选增加指标或 `fatal` 配置开关。

---

## 4. 测试与可观测性

### 4.1 覆盖率数据（2026-06-20 实测）

- `go test ./...` — **全绿**
- `database` 包 — **33.9%** 语句覆盖
- 约 **26 个包无 `_test.go`**，包括：`aof`、`pubsub`、`lib/validate`、`lib/memory`、`datastruct/json|vector|timeseries|probabilistic`、`redis/protocol`、`redis/connection` 等
- `database/` 内约 **38/57 源文件**无对应测试，高风险：`acl.go`、`caching.go`、`stream*.go`、`redisearch*.go`、`json.go`、`vector.go`、`server.go`

**建议优先级：** `lib/validate` → `aof/` → `database/acl.go` + `caching.go` → `redis/parser` Fuzz

### 4.2 测试分布不均

- `datastruct/redisearch/` — 仅 `engine_test.go`
- `datastruct/timeseries/`、`vector/`、`json/` — 0 测试
- `scripting/gopher_engine.go`（500+ 行）— 仅 `engine_test.go`

### 4.3 Fuzz 测试缺位

`redis/parser/` 是网络入口，最值得 `go test -fuzz`：`parserv2.go` 单次解析 + 恶意 RESP 帧不 panic/不死循环。

### 4.4 `go test -race ./...` 缺 CI

`.github/workflows/coverall.yml` 只跑普通测试。建议 nightly 或 PR 门禁。

### 4.5 基准测试缺位

补：`BenchmarkSetGet_Concurrent`、`BenchmarkPubSub_ManySubscribers`、`BenchmarkAOF_Rewrite`。

### 4.6 Prometheus 导出未做

若实施，以 `database/cmdstats.go` + `lib/stats/netstats.go` 为**唯一真源**，避免重复计数。

---

## 5. 文档层

### 5.1 三份核心文档互相矛盾（二次审阅重点）

| 文档 | 问题 |
|------|------|
| **README** | 笼统宣称「Redis 8.x 兼容」，无兼容边界 |
| **AGENTS.md** | 路径过时（`bloom.go`/`probal.go` → 实际 `probabilistic.go`）；Lua 写「内置无依赖」→ 默认 **gopher-lua**（`GODIS_LUA_ENGINE`）；`aclfile` 不存在；Sharded Pub/Sub 文件路径不准 |
| **commands.md** | 更新止于 2024；多处虚列或与代码名不符 |

**建议 README 表述：** 「高覆盖度的 Redis 8.x 协议兼容实现，命令级兼容详见 `docs/COMPATIBILITY.md`」——而非笼统 100% 兼容。

### 5.2 `commands.md` 与代码的具体差异（抽样）

| commands.md 写法 | 实际代码 |
|------------------|----------|
| HEXPIRE 族 | 实现为 **HGETEX/HSETEX/HGETDEL**（`hash_expire.go`） |
| VS.DROPINDEX | **未实现** |
| FT.SYNADD | 仅有 FT.SynUpdate/SynDump |
| CLUSTER MEET/ADDSLOTS 等 15+ 子命令 | 仅 **NODES/INFO/SLOTS/KEYSLOT/HELP**（5 个） |
| SLOWLOG 不支持 | **已实现**（`slowlog.go`） |
| UNWATCH | ACL 分类有，**无执行路径** |
| BITOP / SMOVE / XCLAIM / WAIT 等 | 未实现 |

**代码有但 commands.md 未记：** GETEX/GETDEL、COPY/MOVE/SWAPDB、LMPop/BLMPop/ZMPop、GEOSEARCH、FT.Sug*、SCRIPT DEBUG 系列等。

### 5.3 根目录过程报告应归档

| 文件 | 建议 |
|------|------|
| `改进意见.md` | 保留作 2026-04 历史对照 |
| `Redis8兼容性*.md`（4 份） | 合并为 `docs/COMPATIBILITY.md`；`改进方案.md` 数据已严重过时（称 ~32%） |
| `Lua引擎迁移*.md`、`优化完成报告_*.md`、`改进完成报告.md` | 移入 `docs/history/` |

### 5.4 缺少的文档

- `docs/COMPATIBILITY.md`（或 `docs/redis-differences.md`）— 已知差异总表
- `CHANGELOG.md` — Keep a Changelog 风格
- `example.conf` 补充 `maxmemory`/`maxmemory-policy`/`maxclients` 说明（代码已支持，`config/config.go` 有字段）

### 5.5 `go.mod` 标注

`github.com/yuin/gopher-lua` 标注 `// indirect` 但实际为 `scripting` 直接依赖，应 `go mod tidy` 修正为 direct。

---

## 6. 工具链与 CI

### 6.1 缺 `golangci-lint` / `staticcheck`

`go vet` 已有 4 处 warning（`stream.go` 等），CI 应作为门禁。

### 6.2 CI 使用 Redis 5

`.github/workflows/coverall.yml` 用 Redis 5 sidecar，与 Godis 目标（Redis 8.x）不一致。建议升级 Redis 7/8，或以 godis 自测为主。

### 6.3 缺 release 工作流

`build-all.sh` 存在但无 tag 触发的 GitHub Actions release。建议矩阵：linux/darwin/windows × amd64/arm64。

---

## 7. 建议的实施顺序（合并版）

| 阶段 | 内容 | 估时 | 来源 |
|------|------|------|------|
| **S0 仓库卫生** | `.gitignore`、移除二进制/覆盖率/raft 残留、删 `.travis.yml`、统一 Flag/端口 | 0.5d | 原 §0 |
| **S1 P0 安全** | ACL 启动集成 + 权限检查；修复 AUTH 绕过；HELLO 更新连接状态 | 2d | **二次审阅新增** |
| **S2 关键 TODO** | `Handler.Close` 优雅关闭 + gnet 对齐；`doMigrateSlot` 实现；AOF rewrite 加锁 | 3d | 原 §1 + §1.4 |
| **S3 系统统计** | `INFO` 复制/RDB/AOF 字段；`clientPaused` 加锁 | 1.5d | 原 §1.3 + §2.10 |
| **S4 文档** | 归档根目录报告；新建 `COMPATIBILITY.md` + `CHANGELOG.md`；修正 commands.md/AGENTS.md | 2d | §5 |
| **S5 测试** | Fuzz parser；validate/aof/acl 测试；`-race` CI；database 覆盖率 → 50%+ | 4d | §4 |
| **S6 可选** | Prometheus；config 注入；大文件拆分；release 工作流 | 后续 | 原 P2/P3 |

---

## 8. 其它细枝末节

- `redis/server/gnet/server.go:69` — `c.Write(buffer)` 不检查错误，缺 `stats.RecordOutput`
- `database/acl.go`（命令层）与根包 `acl/acl.go`（引擎层）命名相同、归属不同，文档应明示
- `lib/memory/limiter.go` — `Maxmemory == 0` 时不 Start，关闭路径需确保不分歧
- 运行时错误包装：`cockroachdb/errors.Wrap` 仅 ~20 处（启动路径），复制/脚本等 ~100+ 处仍用 `fmt.Errorf` — 建议复制模块统一 Wrap

---

## 9. 总结

### 强项（保持）

- 命令注册架构清晰，分片锁 + 键排序加锁设计合理
- 功能广度大（JSON/Vector/RediSearch/时序/概率结构/RESP3/客户端缓存）
- 工程化基础已落地（`lib/errs`、`lib/validate`、`lib/memory`、panic 治理、输入校验框架）
- 经典数据类型测试较充分；`go test ./...` 全绿

### 主要待办（按价值排序）

1. **P0 安全：** ACL 启动 + 权限检查；AUTH 绕过修复
2. **S0 仓库卫生**（半小时可完成）
3. **集群迁移** `doMigrateSlot` 实现（否则重平衡不可用）
4. **优雅关闭** + gnet 对齐
5. **文档诚实化：** COMPATIBILITY 矩阵 + commands.md 同步
6. **测试：** Fuzz + race CI + 高风险包补测

### 风险（不建议当前大投入）

- 完全消除 `datastruct/*` 内部 panic（成本高，命令层已校验）
- 完整多 DB 事务（与 Redis 语义不一致，谨慎设计）
- 100% Lua 兼容（gopher-lua 已基本满足 5.1）
- 追求「96–99% 命令兼容」的数字目标（应改为矩阵 + 分模块评估）

### 与 `改进意见.md`（2026-04）的关系

| 主题 | 2026-04 状态 | 2026-06 增量 |
|------|-------------|-------------|
| panic 治理 / 输入校验 | 大部分已落实 | 补：prepare=nil 路径、AUTH/ACL |
| 优雅关闭 / INFO 占位 | 已列 P1，未做 | 仍有效，加入 gnet 对齐 |
| 配置注入 / Prometheus | 已列 P1/P2 | 仍有效 |
| ACL 安全 | **未提及** | **新增 P0** |
| 文档/兼容性 | P3 笼统提及 | **新增具体差异表** |
| 仓库卫生 | 未提及 | **新增 S0** |

---

**文档版本：** 3.1（2026-06-20，合并 Cursor 二次审阅）  
**审阅者：** 原稿（仓库自检）+ Cursor Agent（代码/文档/测试实测对照）  
**说明：** §1 P0 安全项为二次审阅发现的最紧迫缺口；§8 兼容度表述已从「~96–99%」修正为分维度评估。
